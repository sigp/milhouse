-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [milhouse]: function definitions
import Aeneas
import Tree.Types
import Tree.FunsExternal
open Aeneas Aeneas.Std Result ControlFlow Error
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false

/- You can set the `maxHeartbeats` value with the `-max-heartbeats` CLI option -/
set_option maxHeartbeats 1000000

/- You can set the `maxRecDepth` value with the `-max-recdepth` CLI option -/
set_option maxRecDepth 2048

/- You can remove the following line by using the CLI option `-all-computable`: -/
noncomputable section

namespace milhouse

/-- Trait implementation: [core::borrow::{impl core::borrow::Borrow<T> for T}]
    Source: '/rustc/library/core/src/borrow.rs', lines 212:0-212:37
    Name pattern: [core::borrow::Borrow<@T, @T>] -/
@[reducible, rust_trait_impl "core::borrow::Borrow<@T, @T>"]
def core.borrow.Borrow.Blanket (T : Type) : core.borrow.Borrow T T := {
  borrow := core.borrow.Borrow.Blanket.borrow
}

/-- Trait implementation: [core::hash::impls::{impl core::hash::Hash for usize}]
    Source: '/rustc/library/core/src/hash/mod.rs', lines 810:12-810:29
    Name pattern: [core::hash::Hash<usize>] -/
@[reducible, rust_trait_impl "core::hash::Hash<usize>"]
def Usize.Insts.CoreHashHash : core.hash.Hash Std.Usize := {
  hash := fun {H : Type} (HasherInst : core.hash.Hasher H) =>
    Usize.Insts.CoreHashHash.hash HasherInst
}

/-- Trait implementation: [core::hash::impls::{impl core::hash::Hash for (T, B)}]
    Source: '/rustc/library/core/src/hash/mod.rs', lines 890:16-890:60
    Name pattern: [core::hash::Hash<(@T, @B)>] -/
@[reducible, rust_trait_impl "core::hash::Hash<(@T, @B)>"]
def Pair.Insts.CoreHashHash {T : Type} {B : Type} (HashInst : core.hash.Hash T)
  (HashInst1 : core.hash.Hash B) : core.hash.Hash (T × B) := {
  hash := fun {H : Type} (HasherInst : core.hash.Hasher H) =>
    Pair.Insts.CoreHashHash.hash HashInst HashInst1 HasherInst
}

/-- Trait implementation: [core::tuple::{impl core::cmp::PartialEq<(U, T)> for (U, T)}]
    Source: '/rustc/library/core/src/tuple.rs', lines 28:12-28:74
    Name pattern: [core::cmp::PartialEq<(@U, @T), (@U, @T)>] -/
@[reducible, rust_trait_impl "core::cmp::PartialEq<(@U, @T), (@U, @T)>"]
def Pair.Insts.CoreCmpPartialEqPair {U : Type} {T : Type} (cmpPartialEqInst :
  core.cmp.PartialEq U U) (cmpPartialEqInst1 : core.cmp.PartialEq T T) :
  core.cmp.PartialEq (U × T) (U × T) := {
  eq := Pair.Insts.CoreCmpPartialEqPair.eq cmpPartialEqInst cmpPartialEqInst1
}

/-- Trait implementation: [core::tuple::{impl core::cmp::Eq for (U, T)}]
    Source: '/rustc/library/core/src/tuple.rs', lines 44:12-44:60
    Name pattern: [core::cmp::Eq<(@U, @T)>] -/
@[reducible, rust_trait_impl "core::cmp::Eq<(@U, @T)>"]
def Pair.Insts.CoreCmpEq {U : Type} {T : Type} (cmpEqInst : core.cmp.Eq U)
  (cmpEqInst1 : core.cmp.Eq T) : core.cmp.Eq (U × T) := {
  partialEqInst := Pair.Insts.CoreCmpPartialEqPair cmpEqInst.partialEqInst
    cmpEqInst1.partialEqInst
  assert_fields_are_eq := fun _ => ok ()
}

/-- Trait implementation: [core::tuple::{impl core::cmp::PartialOrd<(U, T)> for (U, T)}]
    Source: '/rustc/library/core/src/tuple.rs', lines 66:12-66:76
    Name pattern: [core::cmp::PartialOrd<(@U, @T), (@U, @T)>] -/
@[reducible, rust_trait_impl "core::cmp::PartialOrd<(@U, @T), (@U, @T)>"]
def Pair.Insts.CoreCmpPartialOrdPair {U : Type} {T : Type} (cmpPartialOrdInst :
  core.cmp.PartialOrd U U) (cmpPartialOrdInst1 : core.cmp.PartialOrd T T) :
  core.cmp.PartialOrd (U × T) (U × T) := {
  partialEqInst := Pair.Insts.CoreCmpPartialEqPair
    cmpPartialOrdInst.partialEqInst cmpPartialOrdInst1.partialEqInst
  partial_cmp := Pair.Insts.CoreCmpPartialOrdPair.partial_cmp cmpPartialOrdInst
    cmpPartialOrdInst1
}

/-- Trait implementation: [core::tuple::{impl core::cmp::Ord for (U, T)}]
    Source: '/rustc/library/core/src/tuple.rs', lines 111:12-111:62
    Name pattern: [core::cmp::Ord<(@U, @T)>] -/
@[reducible, rust_trait_impl "core::cmp::Ord<(@U, @T)>"]
def Pair.Insts.CoreCmpOrd {U : Type} {T : Type} (cmpOrdInst : core.cmp.Ord U)
  (cmpOrdInst1 : core.cmp.Ord T) : core.cmp.Ord (U × T) := {
  eqInst := Pair.Insts.CoreCmpEq cmpOrdInst.eqInst cmpOrdInst1.eqInst
  partialOrdInst := Pair.Insts.CoreCmpPartialOrdPair cmpOrdInst.partialOrdInst
    cmpOrdInst1.partialOrdInst
  cmp := Pair.Insts.CoreCmpOrd.cmp cmpOrdInst cmpOrdInst1
}

/-- Trait implementation: [std::hash::random::{impl core::hash::Hasher for std::hash::random::DefaultHasher}]
    Source: '/rustc/library/std/src/hash/random.rs', lines 125:0-125:29
    Name pattern: [core::hash::Hasher<std::hash::random::DefaultHasher>] -/
@[reducible, rust_trait_impl
  "core::hash::Hasher<std::hash::random::DefaultHasher>"]
def std.hash.random.DefaultHasher.Insts.CoreHashHasher : core.hash.Hasher
  std.hash.random.DefaultHasher := {
  finish := std.hash.random.DefaultHasher.Insts.CoreHashHasher.finish
  write := std.hash.random.DefaultHasher.Insts.CoreHashHasher.write
}

/-- Trait implementation: [std::hash::random::{impl core::hash::BuildHasher<std::hash::random::DefaultHasher> for std::hash::random::RandomState}]
    Source: '/rustc/library/std/src/hash/random.rs', lines 80:0-80:32
    Name pattern: [core::hash::BuildHasher<std::hash::random::RandomState, std::hash::random::DefaultHasher>] -/
@[reducible, rust_trait_impl
  "core::hash::BuildHasher<std::hash::random::RandomState, std::hash::random::DefaultHasher>"]
def std.hash.random.RandomState.Insts.CoreHashBuildHasherDefaultHasher :
  core.hash.BuildHasher std.hash.random.RandomState
  std.hash.random.DefaultHasher := {
  HasherInst := std.hash.random.DefaultHasher.Insts.CoreHashHasher
  build_hasher :=
    std.hash.random.RandomState.Insts.CoreHashBuildHasherDefaultHasher.build_hasher
}

/-- Trait implementation: [alloy_primitives::bits::fixed::{impl core::clone::Clone for alloy_primitives::bits::fixed::FixedBytes<N>}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/alloy-primitives-1.0.0/src/bits/fixed.rs', lines 16:4-16:9
    Name pattern: [core::clone::Clone<alloy_primitives::bits::fixed::FixedBytes<@N>>] -/
@[reducible, rust_trait_impl
  "core::clone::Clone<alloy_primitives::bits::fixed::FixedBytes<@N>>"]
def alloy_primitives.bits.fixed.FixedBytes.Insts.CoreCloneClone (N : Std.Usize)
  : core.clone.Clone (alloy_primitives.bits.fixed.FixedBytes N) := {
  clone := alloy_primitives.bits.fixed.FixedBytes.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [alloy_primitives::bits::fixed::{impl core::marker::Copy for alloy_primitives::bits::fixed::FixedBytes<N>}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/alloy-primitives-1.0.0/src/bits/fixed.rs', lines 17:4-17:8
    Name pattern: [core::marker::Copy<alloy_primitives::bits::fixed::FixedBytes<@N>>] -/
@[reducible, rust_trait_impl
  "core::marker::Copy<alloy_primitives::bits::fixed::FixedBytes<@N>>"]
def alloy_primitives.bits.fixed.FixedBytes.Insts.CoreMarkerCopy (N : Std.Usize)
  : core.marker.Copy (alloy_primitives.bits.fixed.FixedBytes N) := {
  cloneInst := alloy_primitives.bits.fixed.FixedBytes.Insts.CoreCloneClone N
}

/-- Trait implementation: [alloy_primitives::bits::fixed::{impl core::cmp::PartialEq<alloy_primitives::bits::fixed::FixedBytes<N>> for alloy_primitives::bits::fixed::FixedBytes<N>}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/alloy-primitives-1.0.0/src/bits/fixed.rs', lines 18:4-18:13
    Name pattern: [core::cmp::PartialEq<alloy_primitives::bits::fixed::FixedBytes<@N>, alloy_primitives::bits::fixed::FixedBytes<@N>>] -/
@[reducible, rust_trait_impl
  "core::cmp::PartialEq<alloy_primitives::bits::fixed::FixedBytes<@N>, alloy_primitives::bits::fixed::FixedBytes<@N>>"]
def alloy_primitives.bits.fixed.FixedBytes.Insts.CoreCmpPartialEqFixedBytes (N
  : Std.Usize) : core.cmp.PartialEq (alloy_primitives.bits.fixed.FixedBytes N)
  (alloy_primitives.bits.fixed.FixedBytes N) := {
  eq :=
    alloy_primitives.bits.fixed.FixedBytes.Insts.CoreCmpPartialEqFixedBytes.eq
}

/-- Trait implementation: [alloy_primitives::bits::fixed::{impl core::cmp::Eq for alloy_primitives::bits::fixed::FixedBytes<N>}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/alloy-primitives-1.0.0/src/bits/fixed.rs', lines 19:4-19:6
    Name pattern: [core::cmp::Eq<alloy_primitives::bits::fixed::FixedBytes<@N>>] -/
@[reducible, rust_trait_impl
  "core::cmp::Eq<alloy_primitives::bits::fixed::FixedBytes<@N>>"]
def alloy_primitives.bits.fixed.FixedBytes.Insts.CoreCmpEq (N : Std.Usize) :
  core.cmp.Eq (alloy_primitives.bits.fixed.FixedBytes N) := {
  partialEqInst :=
    alloy_primitives.bits.fixed.FixedBytes.Insts.CoreCmpPartialEqFixedBytes N
  assert_fields_are_eq :=
    alloy_primitives.bits.fixed.FixedBytes.Insts.CoreCmpEq.assert_fields_are_eq
}

/-- Trait implementation: [alloy_primitives::bits::fixed::{impl core::hash::Hash for alloy_primitives::bits::fixed::FixedBytes<N>}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/alloy-primitives-1.0.0/src/bits/fixed.rs', lines 22:4-22:8
    Name pattern: [core::hash::Hash<alloy_primitives::bits::fixed::FixedBytes<@N>>] -/
@[reducible, rust_trait_impl
  "core::hash::Hash<alloy_primitives::bits::fixed::FixedBytes<@N>>"]
def alloy_primitives.bits.fixed.FixedBytes.Insts.CoreHashHash (N : Std.Usize) :
  core.hash.Hash (alloy_primitives.bits.fixed.FixedBytes N) := {
  hash := fun {H : Type} (corehashHasherInst : core.hash.Hasher H) =>
    alloy_primitives.bits.fixed.FixedBytes.Insts.CoreHashHash.hash
    corehashHasherInst
}

/-- Trait implementation: [alloy_primitives::bits::fixed::{impl core::default::Default for alloy_primitives::bits::fixed::FixedBytes<N>}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/alloy-primitives-1.0.0/src/bits/fixed.rs', lines 39:0-39:46
    Name pattern: [core::default::Default<alloy_primitives::bits::fixed::FixedBytes<@N>>] -/
@[reducible, rust_trait_impl
  "core::default::Default<alloy_primitives::bits::fixed::FixedBytes<@N>>"]
def alloy_primitives.bits.fixed.FixedBytes.Insts.CoreDefaultDefault (N :
  Std.Usize) : core.default.Default (alloy_primitives.bits.fixed.FixedBytes N)
  := {
  default :=
    alloy_primitives.bits.fixed.FixedBytes.Insts.CoreDefaultDefault.default N
}

/-- Trait implementation: [parking_lot::raw_rwlock::{impl lock_api::rwlock::RawRwLock<lock_api::GuardNoSend> for parking_lot::raw_rwlock::RawRwLock}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/parking_lot-0.12.3/src/raw_rwlock.rs', lines 59:0-59:45
    Name pattern: [lock_api::rwlock::RawRwLock<parking_lot::raw_rwlock::RawRwLock, lock_api::GuardNoSend>] -/
@[reducible, rust_trait_impl
  "lock_api::rwlock::RawRwLock<parking_lot::raw_rwlock::RawRwLock, lock_api::GuardNoSend>"]
def parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend :
  lock_api.rwlock.RawRwLock parking_lot.raw_rwlock.RawRwLock
  lock_api.GuardNoSend := {
  INIT :=
    parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend.INIT
  lock_shared :=
    parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend.lock_shared
  try_lock_shared :=
    parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend.try_lock_shared
  unlock_shared :=
    parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend.unlock_shared
  lock_exclusive :=
    parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend.lock_exclusive
  try_lock_exclusive :=
    parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend.try_lock_exclusive
  unlock_exclusive :=
    parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend.unlock_exclusive
}

/-- [milhouse::utils::opt_packing_factor]:
    Source: 'src/utils.rs', lines 67:0-72:1
    Visibility: public -/
def utils.opt_packing_factor
  {T : Type} (tree_hashTreeHashInst : tree_hash.TreeHash T) :
  Result (Option Std.Usize)
  := do
  let tht ← tree_hashTreeHashInst.tree_hash_type
  match tht with
  | tree_hash.TreeHashType.Basic =>
    let i ← tree_hashTreeHashInst.tree_hash_packing_factor
    ok (some i)
  | tree_hash.TreeHashType.Vector => ok none
  | tree_hash.TreeHashType.List => ok none
  | tree_hash.TreeHashType.Container => ok none

/-- [milhouse::utils::int_log]:
    Source: 'src/utils.rs', lines 42:0-47:1
    Visibility: public -/
def utils.int_log (n : Std.Usize) : Result Std.Usize := do
  let o ← core.num.Usize.checked_next_power_of_two n
  match o with
  | none => let i ← core.mem.size_of Std.Usize
            8#usize * i
  | some x =>
    let i ← core.num.Usize.trailing_zeros x
    ok (UScalar.cast .Usize i)

/-- [milhouse::utils::opt_packing_depth]:
    Source: 'src/utils.rs', lines 75:0-78:1
    Visibility: public -/
def utils.opt_packing_depth
  {T : Type} (tree_hashTreeHashInst : tree_hash.TreeHash T) :
  Result (Option Std.Usize)
  := do
  let o ← utils.opt_packing_factor tree_hashTreeHashInst
  let cf ← core.option.Option.Insts.CoreOpsTry_traitTry.branch o
  match cf with
  | core.ops.control_flow.ControlFlow.Continue val =>
    let i ← utils.int_log val
    ok (some i)
  | core.ops.control_flow.ControlFlow.Break residual =>
    core.option.Option.Insts.CoreOpsTry_traitFromResidualOptionInfallible.from_residual
      Std.Usize residual

/-- [milhouse::MAX_TREE_DEPTH]
    Source: 'src/lib.rs', lines 43:0-43:57
    Visibility: public -/
@[global_simps, irreducible]
def MAX_TREE_DEPTH : Result Std.Usize := do
  let i ← lift (UScalar.cast .Usize core.num.U64.BITS)
  i - 1#usize

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::new]:
    Source: 'src/builder.rs', lines 21:4-37:5 -/
def builder.Builder.new
  {T : Type} (ValueInst : Value T) (depth : Std.Usize) (level : Std.Usize) :
  Result (core.result.Result (builder.Builder T) error.Error)
  := do
  let o ← utils.opt_packing_depth ValueInst.tree_hashTreeHashInst
  let packing_depth ← lift (core.option.Option.unwrap_or o 0#usize)
  let i ← lift (core.num.Usize.saturating_add depth packing_depth)
  let i1 ← MAX_TREE_DEPTH
  if i > i1
  then ok (core.result.Result.Err (error.Error.BuilderInvalidDepth depth))
  else
    let i2 ← depth + packing_depth
    let capacity ← 1#usize <<< i2
    let v := alloc.vec.Vec.with_capacity (utils.MaybeArced (tree.Tree T)) depth
    let o1 ← utils.opt_packing_factor ValueInst.tree_hashTreeHashInst
    ok (core.result.Result.Ok
      {
        stack := v,
        depth,
        level,
        length := 0#usize,
        packing_factor := o1,
        packing_depth,
        capacity
      })

/-- [milhouse::utils::{milhouse::utils::Length}::as_usize]:
    Source: 'src/utils.rs', lines 34:4-36:5
    Visibility: public -/
def utils.Length.as_usize (self : utils.Length) : Result Std.Usize := do
  ok self

/-- [milhouse::utils::{milhouse::utils::Length}::as_mut]:
    Source: 'src/utils.rs', lines 29:4-31:5
    Visibility: public -/
def utils.Length.as_mut
  (self : utils.Length) :
  Result (Std.Usize × (Std.Usize → utils.Length))
  := do
  let back := fun i => i
  ok (self, back)

/-- [milhouse::utils::{milhouse::utils::MaybeArced<T>}::arced]:
    Source: 'src/utils.rs', lines 14:4-19:5
    Visibility: public -/
def utils.MaybeArced.arced
  {T : Type} (self : utils.MaybeArced T) : Result (triomphe.arc.Arc T) := do
  match self with
  | utils.MaybeArced.Arced arc => ok arc
  | utils.MaybeArced.Unarced value => triomphe.arc.Arc.new value

/-- [milhouse::leaf::{milhouse::leaf::Leaf<T>}::with_hash]:
    Source: 'src/leaf.rs', lines 34:4-39:5
    Visibility: public -/
def leaf.Leaf.with_hash
  {T : Type} (value : T)
  (hash : alloy_primitives.bits.fixed.FixedBytes 32#usize) :
  Result (leaf.Leaf T)
  := do
  let rl ←
    lock_api.rwlock.RwLock.new
      parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
      hash
  let a ← triomphe.arc.Arc.new value
  ok { hash := rl, value := a }

/-- [milhouse::leaf::{milhouse::leaf::Leaf<T>}::new]:
    Source: 'src/leaf.rs', lines 30:4-32:5
    Visibility: public -/
def leaf.Leaf.new {T : Type} (value : T) : Result (leaf.Leaf T) := do
  let fb ← alloy_primitives.bits.fixed.FixedBytes.ZERO 32#usize
  leaf.Leaf.with_hash value fb

/-- [milhouse::tree::{milhouse::tree::Tree<T>}::leaf_unboxed]:
    Source: 'src/tree.rs', lines 80:4-82:5
    Visibility: public -/
def tree.Tree.leaf_unboxed
  {T : Type} (ValueInst : Value T) (value : T) : Result (tree.Tree T) := do
  let l ← leaf.Leaf.new value
  ok (tree.Tree.Leaf l)

/-- [milhouse::tree::{milhouse::tree::Tree<T>}::node_unboxed]:
    Source: 'src/tree.rs', lines 68:4-74:5
    Visibility: public -/
def tree.Tree.node_unboxed
  {T : Type} (ValueInst : Value T) (left : triomphe.arc.Arc (tree.Tree T))
  (right : triomphe.arc.Arc (tree.Tree T)) :
  Result (tree.Tree T)
  := do
  let fb ← alloy_primitives.bits.fixed.FixedBytes.ZERO 32#usize
  let rl ←
    lock_api.rwlock.RwLock.new
      parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
      fb
  ok (tree.Tree.Node rl left right)

/-- [milhouse::packed_leaf::{milhouse::packed_leaf::PackedLeaf<T>}::push]:
    Source: 'src/packed_leaf.rs', lines 128:4-136:5
    Visibility: public -/
def packed_leaf.PackedLeaf.push
  {T : Type} (tree_hashTreeHashInst : tree_hash.TreeHash T) (corecloneCloneInst
  : core.clone.Clone T) (self : packed_leaf.PackedLeaf T) (value : T) :
  Result ((core.result.Result Unit error.Error) × (packed_leaf.PackedLeaf T))
  := do
  let i := alloc.vec.Vec.len self.values
  let i1 ← tree_hashTreeHashInst.tree_hash_packing_factor
  if i = i1
  then
    let i2 := alloc.vec.Vec.len self.values
    ok (core.result.Result.Err (error.Error.PackedLeafFull i2), self)
  else
    let v ← alloc.vec.Vec.push self.values value
    ok (core.result.Result.Ok (), { self with values := v })

/-- [milhouse::packed_leaf::{milhouse::packed_leaf::PackedLeaf<T>}::single]:
    Source: 'src/packed_leaf.rs', lines 57:4-65:5
    Visibility: public -/
def packed_leaf.PackedLeaf.single
  {T : Type} (tree_hashTreeHashInst : tree_hash.TreeHash T) (corecloneCloneInst
  : core.clone.Clone T) (value : T) :
  Result (packed_leaf.PackedLeaf T)
  := do
  let i ← tree_hashTreeHashInst.tree_hash_packing_factor
  let values := alloc.vec.Vec.with_capacity T i
  let values1 ← alloc.vec.Vec.push values value
  let fb ← alloy_primitives.bits.fixed.FixedBytes.ZERO 32#usize
  let rl ←
    lock_api.rwlock.RwLock.new
      parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
      fb
  ok { hash := rl, values := values1 }

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::push]: loop body 0:
    Source: 'src/builder.rs', lines 64:8-74:5
    Visibility: public -/
@[rust_loop_body]
def builder.Builder.push_loop0.body
  {T : Type} (ValueInst : Value T) (l : utils.Length)
  (iter : core.ops.range.Range Std.U32)
  (v : alloc.vec.Vec (utils.MaybeArced (tree.Tree T))) (t : tree.Tree T) :
  Result (ControlFlow ((core.ops.range.Range Std.U32) × (alloc.vec.Vec
    (utils.MaybeArced (tree.Tree T))) × (tree.Tree T)) ((core.result.Result
    Unit error.Error) × (alloc.vec.Vec (utils.MaybeArced (tree.Tree T))) ×
    utils.Length))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepU32 iter
  match o with
  | none =>
    let v1 ← alloc.vec.Vec.push v (utils.MaybeArced.Unarced t)
    let (i, as_mut_back) ← utils.Length.as_mut l
    let i1 ← i + 1#usize
    let l1 := as_mut_back i1
    ok (done (core.result.Result.Ok (), v1, l1))
  | some _ =>
    let (o1, v1) ← alloc.vec.Vec.pop Global v
    let r ← core.option.Option.ok_or o1 error.Error.BuilderStackEmptyMerge
    let cf ← core.result.Result.Insts.CoreOpsTry.branch r
    match cf with
    | core.ops.control_flow.ControlFlow.Continue val =>
      let a ← utils.MaybeArced.arced val
      let a1 ← utils.MaybeArced.arced (utils.MaybeArced.Unarced t)
      let t1 ← tree.Tree.node_unboxed ValueInst a a1
      ok (cont (iter1, v1, t1))
    | core.ops.control_flow.ControlFlow.Break residual =>
      let r1 ←
        core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
          Unit (core.convert.FromSame error.Error) residual
      ok (done (r1, v1, l))

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::push]: loop 0:
    Source: 'src/builder.rs', lines 64:8-74:5
    Visibility: public -/
@[rust_loop]
def builder.Builder.push_loop0
  {T : Type} (ValueInst : Value T) (iter : core.ops.range.Range Std.U32)
  (v : alloc.vec.Vec (utils.MaybeArced (tree.Tree T))) (l : utils.Length)
  (t : tree.Tree T) :
  Result ((core.result.Result Unit error.Error) × (alloc.vec.Vec
    (utils.MaybeArced (tree.Tree T))) × utils.Length)
  := do
  loop
    (fun (iter1, v1, t1) => builder.Builder.push_loop0.body ValueInst l iter1
      v1 t1)
    (iter, v, t)

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::push]: loop body 1:
    Source: 'src/builder.rs', lines 64:8-74:5
    Visibility: public -/
@[rust_loop_body]
def builder.Builder.push_loop1.body
  {T : Type} (ValueInst : Value T) (l : utils.Length)
  (iter : core.ops.range.Range Std.U32)
  (v : alloc.vec.Vec (utils.MaybeArced (tree.Tree T))) (t : tree.Tree T) :
  Result (ControlFlow ((core.ops.range.Range Std.U32) × (alloc.vec.Vec
    (utils.MaybeArced (tree.Tree T))) × (tree.Tree T)) ((core.result.Result
    Unit error.Error) × (alloc.vec.Vec (utils.MaybeArced (tree.Tree T))) ×
    utils.Length))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepU32 iter
  match o with
  | none =>
    let v1 ← alloc.vec.Vec.push v (utils.MaybeArced.Unarced t)
    let (i, as_mut_back) ← utils.Length.as_mut l
    let i1 ← i + 1#usize
    let l1 := as_mut_back i1
    ok (done (core.result.Result.Ok (), v1, l1))
  | some _ =>
    let (o1, v1) ← alloc.vec.Vec.pop Global v
    let r ← core.option.Option.ok_or o1 error.Error.BuilderStackEmptyMerge
    let cf ← core.result.Result.Insts.CoreOpsTry.branch r
    match cf with
    | core.ops.control_flow.ControlFlow.Continue val =>
      let a ← utils.MaybeArced.arced val
      let a1 ← utils.MaybeArced.arced (utils.MaybeArced.Unarced t)
      let t1 ← tree.Tree.node_unboxed ValueInst a a1
      ok (cont (iter1, v1, t1))
    | core.ops.control_flow.ControlFlow.Break residual =>
      let r1 ←
        core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
          Unit (core.convert.FromSame error.Error) residual
      ok (done (r1, v1, l))

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::push]: loop 1:
    Source: 'src/builder.rs', lines 64:8-74:5
    Visibility: public -/
@[rust_loop]
def builder.Builder.push_loop1
  {T : Type} (ValueInst : Value T) (iter : core.ops.range.Range Std.U32)
  (v : alloc.vec.Vec (utils.MaybeArced (tree.Tree T))) (l : utils.Length)
  (t : tree.Tree T) :
  Result ((core.result.Result Unit error.Error) × (alloc.vec.Vec
    (utils.MaybeArced (tree.Tree T))) × utils.Length)
  := do
  loop
    (fun (iter1, v1, t1) => builder.Builder.push_loop1.body ValueInst l iter1
      v1 t1)
    (iter, v, t)

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::push]: loop body 2:
    Source: 'src/builder.rs', lines 64:8-74:5
    Visibility: public -/
@[rust_loop_body]
def builder.Builder.push_loop2.body
  {T : Type} (ValueInst : Value T) (l : utils.Length)
  (iter : core.ops.range.Range Std.U32)
  (v : alloc.vec.Vec (utils.MaybeArced (tree.Tree T))) (t : tree.Tree T) :
  Result (ControlFlow ((core.ops.range.Range Std.U32) × (alloc.vec.Vec
    (utils.MaybeArced (tree.Tree T))) × (tree.Tree T)) ((core.result.Result
    Unit error.Error) × (alloc.vec.Vec (utils.MaybeArced (tree.Tree T))) ×
    utils.Length))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepU32 iter
  match o with
  | none =>
    let v1 ← alloc.vec.Vec.push v (utils.MaybeArced.Unarced t)
    let (i, as_mut_back) ← utils.Length.as_mut l
    let i1 ← i + 1#usize
    let l1 := as_mut_back i1
    ok (done (core.result.Result.Ok (), v1, l1))
  | some _ =>
    let (o1, v1) ← alloc.vec.Vec.pop Global v
    let r ← core.option.Option.ok_or o1 error.Error.BuilderStackEmptyMerge
    let cf ← core.result.Result.Insts.CoreOpsTry.branch r
    match cf with
    | core.ops.control_flow.ControlFlow.Continue val =>
      let a ← utils.MaybeArced.arced val
      let a1 ← utils.MaybeArced.arced (utils.MaybeArced.Unarced t)
      let t1 ← tree.Tree.node_unboxed ValueInst a a1
      ok (cont (iter1, v1, t1))
    | core.ops.control_flow.ControlFlow.Break residual =>
      let r1 ←
        core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
          Unit (core.convert.FromSame error.Error) residual
      ok (done (r1, v1, l))

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::push]: loop 2:
    Source: 'src/builder.rs', lines 64:8-74:5
    Visibility: public -/
@[rust_loop]
def builder.Builder.push_loop2
  {T : Type} (ValueInst : Value T) (iter : core.ops.range.Range Std.U32)
  (v : alloc.vec.Vec (utils.MaybeArced (tree.Tree T))) (l : utils.Length)
  (t : tree.Tree T) :
  Result ((core.result.Result Unit error.Error) × (alloc.vec.Vec
    (utils.MaybeArced (tree.Tree T))) × utils.Length)
  := do
  loop
    (fun (iter1, v1, t1) => builder.Builder.push_loop2.body ValueInst l iter1
      v1 t1)
    (iter, v, t)

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::push]:
    Source: 'src/builder.rs', lines 39:4-74:5
    Visibility: public -/
def builder.Builder.push
  {T : Type} (ValueInst : Value T) (self : builder.Builder T) (value : T) :
  Result ((core.result.Result Unit error.Error) × (builder.Builder T))
  := do
  let i ← utils.Length.as_usize self.length
  if i = self.capacity
  then ok (core.result.Result.Err error.Error.BuilderFull, self)
  else
    let next_index ← i + 1#usize
    match self.packing_factor with
    | none =>
      let t ← tree.Tree.leaf_unboxed ValueInst value
      let i1 ← core.num.Usize.trailing_zeros next_index
      let i2 ← lift (UScalar.cast .U32 self.packing_depth)
      let values_to_merge ← lift (core.num.U32.saturating_sub i1 i2)
      let (r, v, l) ←
        builder.Builder.push_loop0 ValueInst
          { start := 0#u32, «end» := values_to_merge } self.stack self.length
          t
      ok (r, { self with stack := v, length := l })
    | some packing_factor =>
      let b ← core.num.Usize.is_multiple_of i packing_factor
      if b
      then
        let pl ←
          packed_leaf.PackedLeaf.single ValueInst.tree_hashTreeHashInst
            ValueInst.corecloneCloneInst value
        let i1 ← core.num.Usize.trailing_zeros next_index
        let i2 ← lift (UScalar.cast .U32 self.packing_depth)
        let values_to_merge ← lift (core.num.U32.saturating_sub i1 i2)
        let (r, v, l) ←
          builder.Builder.push_loop1 ValueInst
            { start := 0#u32, «end» := values_to_merge } self.stack
            self.length (tree.Tree.PackedLeaf pl)
        ok (r, { self with stack := v, length := l })
      else
        let (o, v) ← alloc.vec.Vec.pop Global self.stack
        match o with
        | none =>
          ok (core.result.Result.Err error.Error.BuilderExpectedLeaf,
            { self with stack := v })
        | some ma =>
          match ma with
          | utils.MaybeArced.Arced _ =>
            ok (core.result.Result.Err error.Error.BuilderExpectedLeaf,
              { self with stack := v })
          | utils.MaybeArced.Unarced t =>
            match t with
            | tree.Tree.Leaf _ =>
              ok (core.result.Result.Err error.Error.BuilderExpectedLeaf,
                { self with stack := v })
            | tree.Tree.PackedLeaf leaf =>
              let (r, leaf1) ←
                packed_leaf.PackedLeaf.push ValueInst.tree_hashTreeHashInst
                  ValueInst.corecloneCloneInst leaf value
              let cf ← core.result.Result.Insts.CoreOpsTry.branch r
              match cf with
              | core.ops.control_flow.ControlFlow.Continue _ =>
                let i1 ← core.num.Usize.trailing_zeros next_index
                let i2 ← lift (UScalar.cast .U32 self.packing_depth)
                let values_to_merge ←
                  lift (core.num.U32.saturating_sub i1 i2)
                let (r1, v1, l) ←
                  builder.Builder.push_loop2 ValueInst
                    { start := 0#u32, «end» := values_to_merge } v
                    self.length (tree.Tree.PackedLeaf leaf1)
                ok (r1, { self with stack := v1, length := l })
              | core.ops.control_flow.ControlFlow.Break residual =>
                let r1 ←
                  core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                    Unit (core.convert.FromSame error.Error) residual
                ok (r1, { self with stack := v })
            | tree.Tree.Node _ _ _ =>
              ok (core.result.Result.Err error.Error.BuilderExpectedLeaf,
                { self with stack := v })
            | tree.Tree.Zero _ =>
              ok (core.result.Result.Err error.Error.BuilderExpectedLeaf,
                { self with stack := v })

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::push_node]: loop body 0:
    Source: 'src/builder.rs', lines 100:8-105:9 -/
@[rust_loop_body]
def builder.Builder.push_node_loop.body
  {T : Type} (ValueInst : Value T) (iter : core.ops.range.Range Std.U32)
  (v : alloc.vec.Vec (utils.MaybeArced (tree.Tree T)))
  (new_stack_top : utils.MaybeArced (tree.Tree T)) :
  Result (ControlFlow ((core.ops.range.Range Std.U32) × (alloc.vec.Vec
    (utils.MaybeArced (tree.Tree T))) × (utils.MaybeArced (tree.Tree T)))
    ((alloc.vec.Vec (utils.MaybeArced (tree.Tree T))) × (utils.MaybeArced
    (tree.Tree T))))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepU32 iter
  match o with
  | none => ok (done (v, new_stack_top))
  | some _ =>
    let (o1, v1) ← alloc.vec.Vec.pop Global v
    match o1 with
    | none => ok (cont (iter1, v1, new_stack_top))
    | some left =>
      let a ← utils.MaybeArced.arced left
      let a1 ← utils.MaybeArced.arced new_stack_top
      let t ← tree.Tree.node_unboxed ValueInst a a1
      ok (cont (iter1, v1, utils.MaybeArced.Unarced t))

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::push_node]: loop 0:
    Source: 'src/builder.rs', lines 100:8-105:9 -/
@[rust_loop]
def builder.Builder.push_node_loop
  {T : Type} (ValueInst : Value T) (iter : core.ops.range.Range Std.U32)
  (v : alloc.vec.Vec (utils.MaybeArced (tree.Tree T)))
  (new_stack_top : utils.MaybeArced (tree.Tree T)) :
  Result ((alloc.vec.Vec (utils.MaybeArced (tree.Tree T))) × (utils.MaybeArced
    (tree.Tree T)))
  := do
  loop
    (fun (iter1, v1, new_stack_top1) => builder.Builder.push_node_loop.body
      ValueInst iter1 v1 new_stack_top1)
    (iter, v, new_stack_top)

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::push_node]:
    Source: 'src/builder.rs', lines 76:4-111:5 -/
def builder.Builder.push_node
  {T : Type} (ValueInst : Value T) (self : builder.Builder T)
  (node : triomphe.arc.Arc (tree.Tree T)) (len : Std.Usize) :
  Result ((core.result.Result Unit error.Error) × (builder.Builder T))
  := do
  let i ← utils.Length.as_usize self.length
  let o ← lift (Usize.checked_add i len)
  match o with
  | none => ok (core.result.Result.Err error.Error.BuilderFull, self)
  | some new_length =>
    if new_length > self.capacity
    then ok (core.result.Result.Err error.Error.BuilderFull, self)
    else
      let index_on_level ← i >>> self.level
      let next_index_on_level ← index_on_level + 1#usize
      let values_to_merge ←
        if self.level = 0#usize
        then
          do
          let i1 ← core.num.Usize.trailing_zeros next_index_on_level
          let i2 ← lift (UScalar.cast .U32 self.packing_depth)
          ok (core.num.U32.saturating_sub i1 i2)
        else core.num.Usize.trailing_zeros next_index_on_level
      let (v, new_stack_top) ←
        builder.Builder.push_node_loop ValueInst
          { start := 0#u32, «end» := values_to_merge } self.stack
          (utils.MaybeArced.Arced node)
      let v1 ← alloc.vec.Vec.push v new_stack_top
      let (_, as_mut_back) ← utils.Length.as_mut self.length
      let l := as_mut_back new_length
      ok (core.result.Result.Ok (), { self with stack := v1, length := l })

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::finish_packed_leaf]: loop body 0:
    Source: 'src/builder.rs', lines 115:8-125:5 -/
@[rust_loop_body]
def builder.Builder.finish_packed_leaf_loop.body
  {T : Type} (ValueInst : Value T) (next_index_on_level : Std.Usize)
  (self : builder.Builder T) (i : Std.Usize) :
  Result (ControlFlow ((builder.Builder T) × Std.Usize) ((core.result.Result
    Unit error.Error) × (alloc.vec.Vec (utils.MaybeArced (tree.Tree T))) ×
    Std.Usize × Std.Usize × utils.Length × (Option Std.Usize) × Std.Usize
    × Std.Usize))
  := do
  if i < self.depth
  then
    let i1 ← i + self.packing_depth
    let i2 ← next_index_on_level >>> i1
    let i3 ← lift (i2 &&& 1#usize)
    if i3 = 1#usize
    then
      let (o, v) ← alloc.vec.Vec.pop Global self.stack
      let r ←
        core.option.Option.ok_or o error.Error.BuilderStackEmptyMergeRight
      let cf ← core.result.Result.Insts.CoreOpsTry.branch r
      match cf with
      | core.ops.control_flow.ControlFlow.Continue val =>
        let (o1, v1) ← alloc.vec.Vec.pop Global v
        let r1 ←
          core.option.Option.ok_or o1 error.Error.BuilderStackEmptyMergeLeft
        let cf1 ← core.result.Result.Insts.CoreOpsTry.branch r1
        match cf1 with
        | core.ops.control_flow.ControlFlow.Continue val1 =>
          let a ← utils.MaybeArced.arced val1
          let a1 ← utils.MaybeArced.arced val
          let t ← tree.Tree.node_unboxed ValueInst a a1
          let v2 ← alloc.vec.Vec.push v1 (utils.MaybeArced.Unarced t)
          let i4 ← i + 1#usize
          ok (cont ({ self with stack := v2 }, i4))
        | core.ops.control_flow.ControlFlow.Break residual =>
          let r2 ←
            core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
              Unit (core.convert.FromSame error.Error) residual
          ok (done (r2, v1, self.depth, self.level, self.length,
            self.packing_factor, self.packing_depth, self.capacity))
      | core.ops.control_flow.ControlFlow.Break residual =>
        let r1 ←
          core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
            Unit (core.convert.FromSame error.Error) residual
        ok (done (r1, v, self.depth, self.level, self.length,
          self.packing_factor, self.packing_depth, self.capacity))
    else
      ok (done (core.result.Result.Ok (), self.stack, self.depth, self.level,
        self.length, self.packing_factor, self.packing_depth, self.capacity))
  else
    ok (done (core.result.Result.Ok (), self.stack, self.depth, self.level,
      self.length, self.packing_factor, self.packing_depth, self.capacity))

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::finish_packed_leaf]: loop 0:
    Source: 'src/builder.rs', lines 115:8-125:5 -/
@[rust_loop]
def builder.Builder.finish_packed_leaf_loop
  {T : Type} (ValueInst : Value T) (self : builder.Builder T)
  (next_index_on_level : Std.Usize) (i : Std.Usize) :
  Result ((core.result.Result Unit error.Error) × (alloc.vec.Vec
    (utils.MaybeArced (tree.Tree T))) × Std.Usize × Std.Usize × utils.Length
    × (Option Std.Usize) × Std.Usize × Std.Usize)
  := do
  loop
    (fun (self1, i1) => builder.Builder.finish_packed_leaf_loop.body ValueInst
      next_index_on_level self1 i1)
    (self, i)

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::finish_packed_leaf]:
    Source: 'src/builder.rs', lines 113:4-125:5 -/
def builder.Builder.finish_packed_leaf
  {T : Type} (ValueInst : Value T) (self : builder.Builder T)
  (next_index_on_level : Std.Usize) :
  Result ((core.result.Result Unit error.Error) × (builder.Builder T))
  := do
  let (r, v, i, i1, l, o, i2, i3) ←
    builder.Builder.finish_packed_leaf_loop ValueInst self next_index_on_level
      0#usize
  ok (r,
    {
      stack := v,
      depth := i,
      level := i1,
      length := l,
      packing_factor := o,
      packing_depth := i2,
      capacity := i3
    })

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::finish_level]: loop body 0:
    Source: 'src/builder.rs', lines 129:8-144:5 -/
@[rust_loop_body]
def builder.Builder.finish_level_loop.body
  {T : Type} (ValueInst : Value T) (next_index_on_level : Std.Usize)
  (self : builder.Builder T) (i : Std.Usize) :
  Result (ControlFlow ((builder.Builder T) × Std.Usize) ((core.result.Result
    Unit error.Error) × (alloc.vec.Vec (utils.MaybeArced (tree.Tree T))) ×
    Std.Usize × Std.Usize × utils.Length × (Option Std.Usize) × Std.Usize
    × Std.Usize))
  := do
  if i < self.depth
  then
    let i1 ← next_index_on_level <<< self.level
    let i2 ← i + self.packing_depth
    let i3 ← i1 >>> i2
    let i4 ← lift (i3 &&& 1#usize)
    if i4 = 1#usize
    then
      let (o, v) ← alloc.vec.Vec.pop Global self.stack
      let r ←
        core.option.Option.ok_or o error.Error.BuilderStackEmptyFinishRight
      let cf ← core.result.Result.Insts.CoreOpsTry.branch r
      match cf with
      | core.ops.control_flow.ControlFlow.Continue val =>
        let (o1, v1) ← alloc.vec.Vec.pop Global v
        let r1 ←
          core.option.Option.ok_or o1 error.Error.BuilderStackEmptyFinishLeft
        let cf1 ← core.result.Result.Insts.CoreOpsTry.branch r1
        match cf1 with
        | core.ops.control_flow.ControlFlow.Continue val1 =>
          let a ← utils.MaybeArced.arced val1
          let a1 ← utils.MaybeArced.arced val
          let t ← tree.Tree.node_unboxed ValueInst a a1
          let v2 ← alloc.vec.Vec.push v1 (utils.MaybeArced.Unarced t)
          let i5 ← i + 1#usize
          ok (cont ({ self with stack := v2 }, i5))
        | core.ops.control_flow.ControlFlow.Break residual =>
          let r2 ←
            core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
              Unit (core.convert.FromSame error.Error) residual
          ok (done (r2, v1, self.depth, self.level, self.length,
            self.packing_factor, self.packing_depth, self.capacity))
      | core.ops.control_flow.ControlFlow.Break residual =>
        let r1 ←
          core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
            Unit (core.convert.FromSame error.Error) residual
        ok (done (r1, v, self.depth, self.level, self.length,
          self.packing_factor, self.packing_depth, self.capacity))
    else
      ok (done (core.result.Result.Ok (), self.stack, self.depth, self.level,
        self.length, self.packing_factor, self.packing_depth, self.capacity))
  else
    ok (done (core.result.Result.Ok (), self.stack, self.depth, self.level,
      self.length, self.packing_factor, self.packing_depth, self.capacity))

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::finish_level]: loop 0:
    Source: 'src/builder.rs', lines 129:8-144:5 -/
@[rust_loop]
def builder.Builder.finish_level_loop
  {T : Type} (ValueInst : Value T) (self : builder.Builder T)
  (next_index_on_level : Std.Usize) (i : Std.Usize) :
  Result ((core.result.Result Unit error.Error) × (alloc.vec.Vec
    (utils.MaybeArced (tree.Tree T))) × Std.Usize × Std.Usize × utils.Length
    × (Option Std.Usize) × Std.Usize × Std.Usize)
  := do
  loop
    (fun (self1, i1) => builder.Builder.finish_level_loop.body ValueInst
      next_index_on_level self1 i1)
    (self, i)

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::finish_level]:
    Source: 'src/builder.rs', lines 127:4-144:5 -/
def builder.Builder.finish_level
  {T : Type} (ValueInst : Value T) (self : builder.Builder T)
  (next_index_on_level : Std.Usize) (depth : Std.Usize) :
  Result ((core.result.Result Unit error.Error) × (builder.Builder T))
  := do
  let i ← depth + 1#usize
  let (r, v, i1, i2, l, o, i3, i4) ←
    builder.Builder.finish_level_loop ValueInst self next_index_on_level i
  ok (r,
    {
      stack := v,
      depth := i1,
      level := i2,
      length := l,
      packing_factor := o,
      packing_depth := i3,
      capacity := i4
    })

/-- [milhouse::tree::{milhouse::tree::Tree<T>}::zero]:
    Source: 'src/tree.rs', lines 56:4-58:5
    Visibility: public -/
def tree.Tree.zero
  {T : Type} (ValueInst : Value T) (depth : Std.Usize) :
  Result (triomphe.arc.Arc (tree.Tree T))
  := do
  triomphe.arc.Arc.new (tree.Tree.Zero depth)

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::finish_tree]: loop body 0:
    Source: 'src/builder.rs', lines 147:8-165:5 -/
@[rust_loop_body]
def builder.Builder.finish_tree_loop.body
  {T : Type} (ValueInst : Value T) (self : builder.Builder T)
  (next_index_on_level : Std.Usize) :
  Result (ControlFlow ((builder.Builder T) × Std.Usize) ((core.result.Result
    Unit error.Error) × (builder.Builder T)))
  := do
  let i ← next_index_on_level <<< self.level
  if i != self.capacity
  then
    let i1 ← core.num.Usize.trailing_zeros next_index_on_level
    let i2 ← lift (UScalar.cast .Usize i1)
    let i3 ← lift (core.num.Usize.saturating_add i2 self.level)
    let depth ← lift (core.num.Usize.saturating_sub i3 self.packing_depth)
    let (o, v) ← alloc.vec.Vec.pop Global self.stack
    let r ← core.option.Option.ok_or o error.Error.BuilderStackEmptyFinish
    let cf ← core.result.Result.Insts.CoreOpsTry.branch r
    match cf with
    | core.ops.control_flow.ControlFlow.Continue val =>
      let a ← utils.MaybeArced.arced val
      let a1 ← tree.Tree.zero ValueInst depth
      let t ← tree.Tree.node_unboxed ValueInst a a1
      let v1 ← alloc.vec.Vec.push v (utils.MaybeArced.Unarced t)
      let (r1, self1) ←
        builder.Builder.finish_level ValueInst { self with stack := v1 }
          next_index_on_level depth
      let cf1 ← core.result.Result.Insts.CoreOpsTry.branch r1
      match cf1 with
      | core.ops.control_flow.ControlFlow.Continue _ =>
        let i4 ← depth + self1.packing_depth
        let i5 ← i4 - self1.level
        let i6 ← lift (UScalar.cast .U32 i5)
        let i7 ← core.num.Usize.pow 2#usize i6
        let next_index_on_level1 ← next_index_on_level + i7
        ok (cont (self1, next_index_on_level1))
      | core.ops.control_flow.ControlFlow.Break residual =>
        let r2 ←
          core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
            Unit (core.convert.FromSame error.Error) residual
        ok (done (r2, self1))
    | core.ops.control_flow.ControlFlow.Break residual =>
      let r1 ←
        core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
          Unit (core.convert.FromSame error.Error) residual
      ok (done (r1, { self with stack := v }))
  else ok (done (core.result.Result.Ok (), self))

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::finish_tree]: loop 0:
    Source: 'src/builder.rs', lines 147:8-165:5 -/
@[rust_loop]
def builder.Builder.finish_tree_loop
  {T : Type} (ValueInst : Value T) (self : builder.Builder T)
  (next_index_on_level : Std.Usize) :
  Result ((core.result.Result Unit error.Error) × (builder.Builder T))
  := do
  loop
    (fun (self1, next_index_on_level1) => builder.Builder.finish_tree_loop.body
      ValueInst self1 next_index_on_level1)
    (self, next_index_on_level)

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::finish_tree]:
    Source: 'src/builder.rs', lines 146:4-165:5 -/
@[reducible]
def builder.Builder.finish_tree
  {T : Type} (ValueInst : Value T) (self : builder.Builder T)
  (next_index_on_level : Std.Usize) :
  Result ((core.result.Result Unit error.Error) × (builder.Builder T))
  := do
  builder.Builder.finish_tree_loop ValueInst self next_index_on_level

/-- [milhouse::builder::{milhouse::builder::Builder<T>}::finish]:
    Source: 'src/builder.rs', lines 167:4-203:5
    Visibility: public -/
def builder.Builder.finish
  {T : Type} (ValueInst : Value T) (self : builder.Builder T) :
  Result (core.result.Result ((triomphe.arc.Arc (tree.Tree T)) × Std.Usize ×
    utils.Length) error.Error)
  := do
  let b ← alloc.vec.Vec.is_empty Global self.stack
  if b
  then
    let a ← tree.Tree.zero ValueInst self.depth
    ok (core.result.Result.Ok (a, self.depth, 0#usize))
  else
    let length ← utils.Length.as_usize self.length
    let level_capacity ← 1#usize <<< self.level
    let next_index_on_level ← core.num.Usize.div_ceil length level_capacity
    match self.packing_factor with
    | none =>
      let (r, self1) ←
        builder.Builder.finish_tree ValueInst self next_index_on_level
      let cf ← core.result.Result.Insts.CoreOpsTry.branch r
      match cf with
      | core.ops.control_flow.ControlFlow.Continue _ =>
        let (o, v) ← alloc.vec.Vec.pop Global self1.stack
        let r1 ←
          core.option.Option.ok_or o error.Error.BuilderStackEmptyFinalize
        let cf1 ← core.result.Result.Insts.CoreOpsTry.branch r1
        match cf1 with
        | core.ops.control_flow.ControlFlow.Continue val =>
          let tree ← utils.MaybeArced.arced val
          let b1 ← alloc.vec.Vec.is_empty Global v
          if b1
          then ok (core.result.Result.Ok (tree, self1.depth, self1.length))
          else ok (core.result.Result.Err error.Error.BuilderStackLeftover)
        | core.ops.control_flow.ControlFlow.Break residual =>
          core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
            ((triomphe.arc.Arc (tree.Tree T)) × Std.Usize × utils.Length)
            (core.convert.FromSame error.Error) residual
      | core.ops.control_flow.ControlFlow.Break residual =>
        core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
          ((triomphe.arc.Arc (tree.Tree T)) × Std.Usize × utils.Length)
          (core.convert.FromSame error.Error) residual
    | some packing_factor =>
      let i ← length % packing_factor
      let i1 ← lift (core.num.Usize.saturating_sub packing_factor i)
      let skip_indices ← i1 % packing_factor
      if skip_indices > 0#usize
      then
        if self.level = 0#usize
        then
          let (r, self1) ←
            builder.Builder.finish_packed_leaf ValueInst self
              next_index_on_level
          let cf ← core.result.Result.Insts.CoreOpsTry.branch r
          match cf with
          | core.ops.control_flow.ControlFlow.Continue _ =>
            let next_index_on_level1 ← next_index_on_level + skip_indices
            let (r1, self2) ←
              builder.Builder.finish_tree ValueInst self1 next_index_on_level1
            let cf1 ← core.result.Result.Insts.CoreOpsTry.branch r1
            match cf1 with
            | core.ops.control_flow.ControlFlow.Continue _ =>
              let (o, v) ← alloc.vec.Vec.pop Global self2.stack
              let r2 ←
                core.option.Option.ok_or o
                  error.Error.BuilderStackEmptyFinalize
              let cf2 ← core.result.Result.Insts.CoreOpsTry.branch r2
              match cf2 with
              | core.ops.control_flow.ControlFlow.Continue val =>
                let tree ← utils.MaybeArced.arced val
                let b1 ← alloc.vec.Vec.is_empty Global v
                if b1
                then
                  ok (core.result.Result.Ok (tree, self2.depth, self2.length))
                else
                  ok (core.result.Result.Err error.Error.BuilderStackLeftover)
              | core.ops.control_flow.ControlFlow.Break residual =>
                core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                  ((triomphe.arc.Arc (tree.Tree T)) × Std.Usize ×
                  utils.Length) (core.convert.FromSame error.Error) residual
            | core.ops.control_flow.ControlFlow.Break residual =>
              core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                ((triomphe.arc.Arc (tree.Tree T)) × Std.Usize × utils.Length)
                (core.convert.FromSame error.Error) residual
          | core.ops.control_flow.ControlFlow.Break residual =>
            core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
              ((triomphe.arc.Arc (tree.Tree T)) × Std.Usize × utils.Length)
              (core.convert.FromSame error.Error) residual
        else
          let (r, self1) ←
            builder.Builder.finish_tree ValueInst self next_index_on_level
          let cf ← core.result.Result.Insts.CoreOpsTry.branch r
          match cf with
          | core.ops.control_flow.ControlFlow.Continue _ =>
            let (o, v) ← alloc.vec.Vec.pop Global self1.stack
            let r1 ←
              core.option.Option.ok_or o error.Error.BuilderStackEmptyFinalize
            let cf1 ← core.result.Result.Insts.CoreOpsTry.branch r1
            match cf1 with
            | core.ops.control_flow.ControlFlow.Continue val =>
              let tree ← utils.MaybeArced.arced val
              let b1 ← alloc.vec.Vec.is_empty Global v
              if b1
              then ok (core.result.Result.Ok (tree, self1.depth, self1.length))
              else ok (core.result.Result.Err error.Error.BuilderStackLeftover)
            | core.ops.control_flow.ControlFlow.Break residual =>
              core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                ((triomphe.arc.Arc (tree.Tree T)) × Std.Usize × utils.Length)
                (core.convert.FromSame error.Error) residual
          | core.ops.control_flow.ControlFlow.Break residual =>
            core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
              ((triomphe.arc.Arc (tree.Tree T)) × Std.Usize × utils.Length)
              (core.convert.FromSame error.Error) residual
      else
        let (r, self1) ←
          builder.Builder.finish_tree ValueInst self next_index_on_level
        let cf ← core.result.Result.Insts.CoreOpsTry.branch r
        match cf with
        | core.ops.control_flow.ControlFlow.Continue _ =>
          let (o, v) ← alloc.vec.Vec.pop Global self1.stack
          let r1 ←
            core.option.Option.ok_or o error.Error.BuilderStackEmptyFinalize
          let cf1 ← core.result.Result.Insts.CoreOpsTry.branch r1
          match cf1 with
          | core.ops.control_flow.ControlFlow.Continue val =>
            let tree ← utils.MaybeArced.arced val
            let b1 ← alloc.vec.Vec.is_empty Global v
            if b1
            then ok (core.result.Result.Ok (tree, self1.depth, self1.length))
            else ok (core.result.Result.Err error.Error.BuilderStackLeftover)
          | core.ops.control_flow.ControlFlow.Break residual =>
            core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
              ((triomphe.arc.Arc (tree.Tree T)) × Std.Usize × utils.Length)
              (core.convert.FromSame error.Error) residual
        | core.ops.control_flow.ControlFlow.Break residual =>
          core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
            ((triomphe.arc.Arc (tree.Tree T)) × Std.Usize × utils.Length)
            (core.convert.FromSame error.Error) residual

/-- [milhouse::leaf::{impl core::clone::Clone for milhouse::leaf::Leaf<T>}::clone]:
    Source: 'src/leaf.rs', lines 21:4-26:5
    Visibility: public -/
def leaf.Leaf.Insts.CoreCloneClone.clone
  {T : Type} (corecloneCloneInst : core.clone.Clone T) (self : leaf.Leaf T) :
  Result (leaf.Leaf T)
  := do
  let rlrg ←
    lock_api.rwlock.RwLock.read
      parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
      self.hash
  let fb ←
    lock_api.rwlock.RwLockReadGuard.Insts.CoreOpsDerefDeref.deref
      parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
      rlrg
  let rl ←
    lock_api.rwlock.RwLock.new
      parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
      fb
  let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone self.value
  ok { hash := rl, value := a }

/-- [milhouse::packed_leaf::{impl core::clone::Clone for milhouse::packed_leaf::PackedLeaf<T>}::clone]:
    Source: 'src/packed_leaf.rs', lines 20:4-25:5
    Visibility: public -/
def packed_leaf.PackedLeaf.Insts.CoreCloneClone.clone
  {T : Type} (tree_hashTreeHashInst : tree_hash.TreeHash T) (corecloneCloneInst
  : core.clone.Clone T) (self : packed_leaf.PackedLeaf T) :
  Result (packed_leaf.PackedLeaf T)
  := do
  let rlrg ←
    lock_api.rwlock.RwLock.read
      parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
      self.hash
  let fb ←
    lock_api.rwlock.RwLockReadGuard.Insts.CoreOpsDerefDeref.deref
      parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
      rlrg
  let rl ←
    lock_api.rwlock.RwLock.new
      parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
      fb
  let v ← alloc.vec.CloneVec.clone corecloneCloneInst self.values
  ok { hash := rl, values := v }

/-- [milhouse::packed_leaf::{milhouse::packed_leaf::PackedLeaf<T>}::empty]:
    Source: 'src/packed_leaf.rs', lines 50:4-55:5
    Visibility: public -/
def packed_leaf.PackedLeaf.empty
  {T : Type} (tree_hashTreeHashInst : tree_hash.TreeHash T) (corecloneCloneInst
  : core.clone.Clone T) :
  Result (packed_leaf.PackedLeaf T)
  := do
  let fb ← alloy_primitives.bits.fixed.FixedBytes.ZERO 32#usize
  let rl ←
    lock_api.rwlock.RwLock.new
      parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
      fb
  let i ← tree_hashTreeHashInst.tree_hash_packing_factor
  let v := alloc.vec.Vec.with_capacity T i
  ok { hash := rl, values := v }

/-- [milhouse::packed_leaf::{milhouse::packed_leaf::PackedLeaf<T>}::insert_mut]:
    Source: 'src/packed_leaf.rs', lines 111:4-126:5
    Visibility: public -/
def packed_leaf.PackedLeaf.insert_mut
  {T : Type} (tree_hashTreeHashInst : tree_hash.TreeHash T) (corecloneCloneInst
  : core.clone.Clone T) (self : packed_leaf.PackedLeaf T)
  (sub_index : Std.Usize) (value : T) :
  Result ((core.result.Result Unit error.Error) × (packed_leaf.PackedLeaf T))
  := do
  let (_, get_mut_back) ←
    lock_api.rwlock.RwLock.get_mut
      parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
      self.hash
  let fb ← alloy_primitives.bits.fixed.FixedBytes.ZERO 32#usize
  let i := alloc.vec.Vec.len self.values
  if sub_index = i
  then
    let v ← alloc.vec.Vec.push self.values value
    let rl := get_mut_back fb
    ok (core.result.Result.Ok (), { hash := rl, values := v })
  else
    let i1 := alloc.vec.Vec.len self.values
    if sub_index < i1
    then
      let (_, index_mut_back) ←
        alloc.vec.Vec.index_mut (core.slice.index.SliceIndexUsizeSlice T)
          self.values sub_index
      let rl := get_mut_back fb
      let v := index_mut_back value
      ok (core.result.Result.Ok (), { hash := rl, values := v })
    else
      let i2 := alloc.vec.Vec.len self.values
      let rl := get_mut_back fb
      ok (core.result.Result.Err (error.Error.PackedLeafOutOfBounds sub_index
        i2), { self with hash := rl })

/-- [milhouse::packed_leaf::{milhouse::packed_leaf::PackedLeaf<T>}::insert_at_index]:
    Source: 'src/packed_leaf.rs', lines 75:4-83:5
    Visibility: public -/
def packed_leaf.PackedLeaf.insert_at_index
  {T : Type} (tree_hashTreeHashInst : tree_hash.TreeHash T) (corecloneCloneInst
  : core.clone.Clone T) (self : packed_leaf.PackedLeaf T) (index : Std.Usize)
  (value : T) :
  Result (core.result.Result (packed_leaf.PackedLeaf T) error.Error)
  := do
  let fb ← alloy_primitives.bits.fixed.FixedBytes.ZERO 32#usize
  let rl ←
    lock_api.rwlock.RwLock.new
      parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
      fb
  let v ← alloc.vec.CloneVec.clone corecloneCloneInst self.values
  let i ← tree_hashTreeHashInst.tree_hash_packing_factor
  let sub_index ← index % i
  let (r, updated) ←
    packed_leaf.PackedLeaf.insert_mut tree_hashTreeHashInst corecloneCloneInst
      { hash := rl, values := v } sub_index value
  let cf ← core.result.Result.Insts.CoreOpsTry.branch r
  match cf with
  | core.ops.control_flow.ControlFlow.Continue _ =>
    ok (core.result.Result.Ok updated)
  | core.ops.control_flow.ControlFlow.Break residual =>
    core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
      (packed_leaf.PackedLeaf T) (core.convert.FromSame error.Error) residual

/-- [milhouse::packed_leaf::{milhouse::packed_leaf::PackedLeaf<T>}::update]: loop body 0:
    Source: 'src/packed_leaf.rs', lines 102:8-109:5
    Visibility: public -/
@[rust_loop_body]
def packed_leaf.PackedLeaf.update_loop.body
  {T : Type} {U : Type} (tree_hashTreeHashInst : tree_hash.TreeHash T)
  (corecloneCloneInst : core.clone.Clone T) (update_mapUpdateMapInst :
  update_map.UpdateMap U T) (updates : U) (packing_factor : Std.Usize)
  (end1 : Std.Usize) (updated : packed_leaf.PackedLeaf T) (index : Std.Usize) :
  Result (ControlFlow ((packed_leaf.PackedLeaf T) × Std.Usize)
    (core.result.Result (packed_leaf.PackedLeaf T) error.Error))
  := do
  if index < end1
  then
    let o ← update_mapUpdateMapInst.get updates index
    match o with
    | none => let index1 ← index + 1#usize
              ok (cont (updated, index1))
    | some value =>
      let i ← index % packing_factor
      let t ← corecloneCloneInst.clone value
      let (r, updated1) ←
        packed_leaf.PackedLeaf.insert_mut tree_hashTreeHashInst
          corecloneCloneInst updated i t
      let cf ← core.result.Result.Insts.CoreOpsTry.branch r
      match cf with
      | core.ops.control_flow.ControlFlow.Continue _ =>
        let index1 ← index + 1#usize
        ok (cont (updated1, index1))
      | core.ops.control_flow.ControlFlow.Break residual =>
        let r1 ←
          core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
            (packed_leaf.PackedLeaf T) (core.convert.FromSame error.Error)
            residual
        ok (done r1)
  else ok (done (core.result.Result.Ok updated))

/-- [milhouse::packed_leaf::{milhouse::packed_leaf::PackedLeaf<T>}::update]: loop 0:
    Source: 'src/packed_leaf.rs', lines 102:8-109:5
    Visibility: public -/
@[rust_loop]
def packed_leaf.PackedLeaf.update_loop
  {T : Type} {U : Type} (tree_hashTreeHashInst : tree_hash.TreeHash T)
  (corecloneCloneInst : core.clone.Clone T) (update_mapUpdateMapInst :
  update_map.UpdateMap U T) (updates : U) (updated : packed_leaf.PackedLeaf T)
  (packing_factor : Std.Usize) (end1 : Std.Usize) (index : Std.Usize) :
  Result (core.result.Result (packed_leaf.PackedLeaf T) error.Error)
  := do
  loop
    (fun (updated1, index1) => packed_leaf.PackedLeaf.update_loop.body
      tree_hashTreeHashInst corecloneCloneInst update_mapUpdateMapInst updates
      packing_factor end1 updated1 index1)
    (updated, index)

/-- [milhouse::packed_leaf::{milhouse::packed_leaf::PackedLeaf<T>}::update]:
    Source: 'src/packed_leaf.rs', lines 85:4-109:5
    Visibility: public -/
def packed_leaf.PackedLeaf.update
  {T : Type} {U : Type} (tree_hashTreeHashInst : tree_hash.TreeHash T)
  (corecloneCloneInst : core.clone.Clone T) (update_mapUpdateMapInst :
  update_map.UpdateMap U T) (self : packed_leaf.PackedLeaf T)
  (prefix1 : Std.Usize)
  (hash : alloy_primitives.bits.fixed.FixedBytes 32#usize) (updates : U) :
  Result (core.result.Result (packed_leaf.PackedLeaf T) error.Error)
  := do
  let rl ←
    lock_api.rwlock.RwLock.new
      parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
      hash
  let v ← alloc.vec.CloneVec.clone corecloneCloneInst self.values
  let packing_factor ← tree_hashTreeHashInst.tree_hash_packing_factor
  let end1 ← prefix1 + packing_factor
  packed_leaf.PackedLeaf.update_loop tree_hashTreeHashInst corecloneCloneInst
    update_mapUpdateMapInst updates { hash := rl, values := v } packing_factor
    end1 prefix1

/-- [milhouse::tree::{impl core::hash::Hash for milhouse::tree::Tree<T>}::hash]:
    Source: 'src/tree.rs', lines 10:16-10:21
    Visibility: public -/
def tree.Tree.Insts.CoreHashHash.hash
  {T : Type} {H : Type} (ValueInst : Value T) (corehashHashLeafInst :
  core.hash.Hash (leaf.Leaf T)) (corehashHashPackedLeafInst : core.hash.Hash
  (packed_leaf.PackedLeaf T)) (corehashHashArcTreeInst : core.hash.Hash
  (triomphe.arc.Arc (tree.Tree T))) (corehashHashUsizeInst : core.hash.Hash
  Std.Usize) (corehashHasherInst : core.hash.Hasher H) (self : tree.Tree T)
  (state : H) :
  Result H
  := do
  match self with
  | tree.Tree.Leaf _0 =>
    let state1 ←
      Usize.Insts.CoreHashHash.hash corehashHasherInst 0#usize state
    corehashHashLeafInst.hash corehashHasherInst _0 state1
  | tree.Tree.PackedLeaf _0 =>
    let state1 ←
      Usize.Insts.CoreHashHash.hash corehashHasherInst 1#usize state
    corehashHashPackedLeafInst.hash corehashHasherInst _0 state1
  | tree.Tree.Node _ v_left v_right =>
    let state1 ←
      Usize.Insts.CoreHashHash.hash corehashHasherInst 2#usize state
    let state2 ←
      corehashHashArcTreeInst.hash corehashHasherInst v_left state1
    corehashHashArcTreeInst.hash corehashHasherInst v_right state2
  | tree.Tree.Zero _0 =>
    let state1 ←
      Usize.Insts.CoreHashHash.hash corehashHasherInst 3#usize state
    Usize.Insts.CoreHashHash.hash corehashHasherInst _0 state1

/-- Trait implementation: [milhouse::tree::{impl core::hash::Hash for milhouse::tree::Tree<T>}]
    Source: 'src/tree.rs', lines 10:16-10:21 -/
@[reducible]
def tree.Tree.Insts.CoreHashHash {T : Type} (ValueInst : Value T)
  (corehashHashLeafInst : core.hash.Hash (leaf.Leaf T))
  (corehashHashPackedLeafInst : core.hash.Hash (packed_leaf.PackedLeaf T))
  (corehashHashArcTreeInst : core.hash.Hash (triomphe.arc.Arc (tree.Tree T)))
  (corehashHashUsizeInst : core.hash.Hash Std.Usize) : core.hash.Hash
  (tree.Tree T) := {
  hash := fun {H : Type} (corehashHasherInst : core.hash.Hasher H) =>
    tree.Tree.Insts.CoreHashHash.hash ValueInst corehashHashLeafInst
    corehashHashPackedLeafInst corehashHashArcTreeInst Usize.Insts.CoreHashHash
    corehashHasherInst
}

/-- [milhouse::tree::{impl core::clone::Clone for milhouse::tree::Tree<T>}::clone]:
    Source: 'src/tree.rs', lines 29:4-40:5
    Visibility: public -/
def tree.Tree.Insts.CoreCloneClone.clone
  {T : Type} (ValueInst : Value T) (self : tree.Tree T) :
  Result (tree.Tree T)
  := do
  match self with
  | tree.Tree.Leaf l =>
    let l1 ←
      leaf.Leaf.Insts.CoreCloneClone.clone ValueInst.corecloneCloneInst l
    ok (tree.Tree.Leaf l1)
  | tree.Tree.PackedLeaf pl =>
    let pl1 ←
      packed_leaf.PackedLeaf.Insts.CoreCloneClone.clone
        ValueInst.tree_hashTreeHashInst ValueInst.corecloneCloneInst pl
    ok (tree.Tree.PackedLeaf pl1)
  | tree.Tree.Node hash left right =>
    let rlrg ←
      lock_api.rwlock.RwLock.read
        parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
        hash
    let fb ←
      lock_api.rwlock.RwLockReadGuard.Insts.CoreOpsDerefDeref.deref
        parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
        rlrg
    let rl ←
      lock_api.rwlock.RwLock.new
        parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
        fb
    let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone left
    let a1 ← triomphe.arc.Arc.Insts.CoreCloneClone.clone right
    ok (tree.Tree.Node rl a a1)
  | tree.Tree.Zero _ => ok self

/-- Trait implementation: [milhouse::tree::{impl core::clone::Clone for milhouse::tree::Tree<T>}]
    Source: 'src/tree.rs', lines 28:0-41:1 -/
@[reducible]
def tree.Tree.Insts.CoreCloneClone {T : Type} (ValueInst : Value T) :
  core.clone.Clone (tree.Tree T) := {
  clone := tree.Tree.Insts.CoreCloneClone.clone ValueInst
}

/-- [milhouse::tree::{milhouse::tree::Tree<T>}::empty]:
    Source: 'src/tree.rs', lines 44:4-46:5
    Visibility: public -/
def tree.Tree.empty
  {T : Type} (ValueInst : Value T) (depth : Std.Usize) :
  Result (triomphe.arc.Arc (tree.Tree T))
  := do
  tree.Tree.zero ValueInst depth

/-- [milhouse::tree::{milhouse::tree::Tree<T>}::node]:
    Source: 'src/tree.rs', lines 48:4-54:5
    Visibility: public -/
def tree.Tree.node
  {T : Type} (ValueInst : Value T) (left : triomphe.arc.Arc (tree.Tree T))
  (right : triomphe.arc.Arc (tree.Tree T))
  (hash : alloy_primitives.bits.fixed.FixedBytes 32#usize) :
  Result (triomphe.arc.Arc (tree.Tree T))
  := do
  let rl ←
    lock_api.rwlock.RwLock.new
      parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
      hash
  triomphe.arc.Arc.new (tree.Tree.Node rl left right)

/-- [milhouse::tree::{milhouse::tree::Tree<T>}::leaf]:
    Source: 'src/tree.rs', lines 60:4-62:5
    Visibility: public -/
def tree.Tree.leaf
  {T : Type} (ValueInst : Value T) (value : T) :
  Result (triomphe.arc.Arc (tree.Tree T))
  := do
  let l ← leaf.Leaf.new value
  triomphe.arc.Arc.new (tree.Tree.Leaf l)

/-- [milhouse::tree::{milhouse::tree::Tree<T>}::leaf_with_hash]:
    Source: 'src/tree.rs', lines 64:4-66:5
    Visibility: public -/
def tree.Tree.leaf_with_hash
  {T : Type} (ValueInst : Value T) (value : T)
  (hash : alloy_primitives.bits.fixed.FixedBytes 32#usize) :
  Result (triomphe.arc.Arc (tree.Tree T))
  := do
  let l ← leaf.Leaf.with_hash value hash
  triomphe.arc.Arc.new (tree.Tree.Leaf l)

/-- [milhouse::tree::{milhouse::tree::Tree<T>}::zero_unboxed]:
    Source: 'src/tree.rs', lines 76:4-78:5
    Visibility: public -/
def tree.Tree.zero_unboxed
  {T : Type} (ValueInst : Value T) (depth : Std.Usize) :
  Result (tree.Tree T)
  := do
  ok (tree.Tree.Zero depth)

/-- [milhouse::tree::{milhouse::tree::Tree<T>}::get_recursive]:
    Source: 'src/tree.rs', lines 84:4-103:5
    Visibility: public -/
def tree.Tree.get_recursive
  {T : Type} (ValueInst : Value T) (self : tree.Tree T) (index : Std.Usize)
  (depth : Std.Usize) (packing_depth : Std.Usize) :
  Result (Option T)
  := do
  match self with
  | tree.Tree.Leaf l =>
    if depth = 0#usize
    then
      let t ← triomphe.arc.Arc.Insts.CoreOpsDerefDeref.deref l.value
      ok (some t)
    else ok none
  | tree.Tree.PackedLeaf pl =>
    if depth = 0#usize
    then
      let s := alloc.vec.Vec.deref pl.values
      let i ← ValueInst.tree_hashTreeHashInst.tree_hash_packing_factor
      let i1 ← index % i
      core.slice.Slice.get (core.slice.index.SliceIndexUsizeSlice T) s i1
    else ok none
  | tree.Tree.Node _ left right =>
    if depth > 0#usize
    then
      let new_depth ← depth - 1#usize
      let i ← new_depth + packing_depth
      let i1 ← index >>> i
      let i2 ← lift (i1 &&& 1#usize)
      if i2 = 0#usize
      then
        let t ← triomphe.arc.Arc.Insts.CoreOpsDerefDeref.deref left
        tree.Tree.get_recursive ValueInst t index new_depth packing_depth
      else
        let t ← triomphe.arc.Arc.Insts.CoreOpsDerefDeref.deref right
        tree.Tree.get_recursive ValueInst t index new_depth packing_depth
    else ok none
  | tree.Tree.Zero _ => ok none
partial_fixpoint

/-- [milhouse::tree::{milhouse::tree::Tree<T>}::with_updated_leaf]:
    Source: 'src/tree.rs', lines 108:4-155:5
    Visibility: public -/
def tree.Tree.with_updated_leaf
  {T : Type} (ValueInst : Value T) (self : tree.Tree T) (index : Std.Usize)
  (new_value : T) (depth : Std.Usize) :
  Result (core.result.Result (triomphe.arc.Arc (tree.Tree T)) error.Error)
  := do
  match self with
  | tree.Tree.Leaf _ =>
    if depth = 0#usize
    then
      let a ← tree.Tree.leaf ValueInst new_value
      ok (core.result.Result.Ok a)
    else ok (core.result.Result.Err error.Error.UpdateLeafError)
  | tree.Tree.PackedLeaf leaf =>
    if depth = 0#usize
    then
      let r ←
        packed_leaf.PackedLeaf.insert_at_index ValueInst.tree_hashTreeHashInst
          ValueInst.corecloneCloneInst leaf index new_value
      let cf ← core.result.Result.Insts.CoreOpsTry.branch r
      match cf with
      | core.ops.control_flow.ControlFlow.Continue val =>
        let a ← triomphe.arc.Arc.new (tree.Tree.PackedLeaf val)
        ok (core.result.Result.Ok a)
      | core.ops.control_flow.ControlFlow.Break residual =>
        core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
          (triomphe.arc.Arc (tree.Tree T)) (core.convert.FromSame error.Error)
          residual
    else ok (core.result.Result.Err error.Error.UpdateLeafError)
  | tree.Tree.Node _ left right =>
    if depth > 0#usize
    then
      let o ← utils.opt_packing_depth ValueInst.tree_hashTreeHashInst
      let packing_depth ← lift (core.option.Option.unwrap_or o 0#usize)
      let new_depth ← depth - 1#usize
      let i ← new_depth + packing_depth
      let i1 ← index >>> i
      let i2 ← lift (i1 &&& 1#usize)
      if i2 = 0#usize
      then
        let t ← triomphe.arc.Arc.Insts.CoreOpsDerefDeref.deref left
        let r ←
          tree.Tree.with_updated_leaf ValueInst t index new_value new_depth
        let cf ← core.result.Result.Insts.CoreOpsTry.branch r
        match cf with
        | core.ops.control_flow.ControlFlow.Continue val =>
          let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone right
          let fb ← alloy_primitives.bits.fixed.FixedBytes.ZERO 32#usize
          let a1 ← tree.Tree.node ValueInst val a fb
          ok (core.result.Result.Ok a1)
        | core.ops.control_flow.ControlFlow.Break residual =>
          core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
            (triomphe.arc.Arc (tree.Tree T)) (core.convert.FromSame
            error.Error) residual
      else
        let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone left
        let t ← triomphe.arc.Arc.Insts.CoreOpsDerefDeref.deref right
        let r ←
          tree.Tree.with_updated_leaf ValueInst t index new_value new_depth
        let cf ← core.result.Result.Insts.CoreOpsTry.branch r
        match cf with
        | core.ops.control_flow.ControlFlow.Continue val =>
          let fb ← alloy_primitives.bits.fixed.FixedBytes.ZERO 32#usize
          let a1 ← tree.Tree.node ValueInst a val fb
          ok (core.result.Result.Ok a1)
        | core.ops.control_flow.ControlFlow.Break residual =>
          core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
            (triomphe.arc.Arc (tree.Tree T)) (core.convert.FromSame
            error.Error) residual
    else ok (core.result.Result.Err error.Error.UpdateLeafError)
  | tree.Tree.Zero zero_depth =>
    if zero_depth = depth
    then
      if depth = 0#usize
      then
        let o ← utils.opt_packing_factor ValueInst.tree_hashTreeHashInst
        let b := core.option.Option.is_some o
        if b
        then
          let pl ←
            packed_leaf.PackedLeaf.single ValueInst.tree_hashTreeHashInst
              ValueInst.corecloneCloneInst new_value
          let a ← triomphe.arc.Arc.new (tree.Tree.PackedLeaf pl)
          ok (core.result.Result.Ok a)
        else
          let a ← tree.Tree.leaf ValueInst new_value
          ok (core.result.Result.Ok a)
      else
        let i ← depth - 1#usize
        let new_zero ← tree.Tree.zero ValueInst i
        let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone new_zero
        let fb ← alloy_primitives.bits.fixed.FixedBytes.ZERO 32#usize
        let a1 ← tree.Tree.node ValueInst a new_zero fb
        let t ← triomphe.arc.Arc.Insts.CoreOpsDerefDeref.deref a1
        tree.Tree.with_updated_leaf ValueInst t index new_value depth
    else ok (core.result.Result.Err error.Error.UpdateLeafError)
partial_fixpoint

/-- [milhouse::utils::opt_hash]:
    Source: 'src/utils.rs', lines 93:0-99:1
    Visibility: public -/
def utils.opt_hash
  (hashes : Option (alloc.collections.btree.map.BTreeMap (Std.Usize ×
  Std.Usize) (alloy_primitives.bits.fixed.FixedBytes 32#usize) Global))
  (depth : Std.Usize) (prefix1 : Std.Usize) :
  Result (Option (alloy_primitives.bits.fixed.FixedBytes 32#usize))
  := do
  let cf ← core.option.Option.Insts.CoreOpsTry_traitTry.branch hashes
  match cf with
  | core.ops.control_flow.ControlFlow.Continue val =>
    let o ←
      alloc.collections.btree.map.BTreeMap.get core.core.clone.CloneGlobal
        (core.borrow.Borrow.Blanket (Std.Usize × Std.Usize))
        (Pair.Insts.CoreCmpOrd core.cmp.OrdUsize core.cmp.OrdUsize)
        (Pair.Insts.CoreCmpOrd core.cmp.OrdUsize core.cmp.OrdUsize) val (depth,
        prefix1)
    core.option.OptionShared0T.copied
      (alloy_primitives.bits.fixed.FixedBytes.Insts.CoreMarkerCopy 32#usize) o
  | core.ops.control_flow.ControlFlow.Break residual =>
    core.option.Option.Insts.CoreOpsTry_traitFromResidualOptionInfallible.from_residual
      (alloy_primitives.bits.fixed.FixedBytes 32#usize) residual

/-- [milhouse::tree::{milhouse::tree::Tree<T>}::with_updated_leaves]:
    Source: 'src/tree.rs', lines 157:4-230:5
    Visibility: public -/
def tree.Tree.with_updated_leaves
  {T : Type} {U : Type} (ValueInst : Value T) (update_mapUpdateMapInst :
  update_map.UpdateMap U T) (self : tree.Tree T) (updates : U)
  (prefix1 : Std.Usize) (depth : Std.Usize)
  (hashes : Option (alloc.collections.btree.map.BTreeMap (Std.Usize ×
  Std.Usize) (alloy_primitives.bits.fixed.FixedBytes 32#usize) Global)) :
  Result (core.result.Result (triomphe.arc.Arc (tree.Tree T)) error.Error)
  := do
  let o ← utils.opt_hash hashes depth prefix1
  let hash ←
    core.option.Option.unwrap_or_default
      (alloy_primitives.bits.fixed.FixedBytes.Insts.CoreDefaultDefault
      32#usize) o
  match self with
  | tree.Tree.Leaf _ =>
    if depth = 0#usize
    then
      let o1 ← update_mapUpdateMapInst.get updates prefix1
      let o2 ←
        core.option.OptionShared0T.cloned ValueInst.corecloneCloneInst o1
      let r ←
        core.option.Option.ok_or o2 (error.Error.LeafUpdateMissing prefix1)
      let cf ← core.result.Result.Insts.CoreOpsTry.branch r
      match cf with
      | core.ops.control_flow.ControlFlow.Continue val =>
        let a ← tree.Tree.leaf_with_hash ValueInst val hash
        ok (core.result.Result.Ok a)
      | core.ops.control_flow.ControlFlow.Break residual =>
        core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
          (triomphe.arc.Arc (tree.Tree T)) (core.convert.FromSame error.Error)
          residual
    else ok (core.result.Result.Err error.Error.UpdateLeavesError)
  | tree.Tree.PackedLeaf pl =>
    if depth = 0#usize
    then
      let r ←
        packed_leaf.PackedLeaf.update ValueInst.tree_hashTreeHashInst
          ValueInst.corecloneCloneInst update_mapUpdateMapInst pl prefix1 hash
          updates
      let cf ← core.result.Result.Insts.CoreOpsTry.branch r
      match cf with
      | core.ops.control_flow.ControlFlow.Continue val =>
        let a ← triomphe.arc.Arc.new (tree.Tree.PackedLeaf val)
        ok (core.result.Result.Ok a)
      | core.ops.control_flow.ControlFlow.Break residual =>
        core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
          (triomphe.arc.Arc (tree.Tree T)) (core.convert.FromSame error.Error)
          residual
    else ok (core.result.Result.Err error.Error.UpdateLeavesError)
  | tree.Tree.Node _ left right =>
    if depth > 0#usize
    then
      let o1 ← utils.opt_packing_depth ValueInst.tree_hashTreeHashInst
      let packing_depth ← lift (core.option.Option.unwrap_or o1 0#usize)
      let new_depth ← depth - 1#usize
      let i ← new_depth + packing_depth
      let i1 ← 1#usize <<< i
      let right_prefix ← lift (prefix1 ||| i1)
      let i2 ← depth + packing_depth
      let i3 ← 1#usize <<< i2
      let right_subtree_end ← prefix1 + i3
      let has_left_updates ←
        update_mapUpdateMapInst.has_any_in_range updates prefix1 right_prefix
      let has_right_updates ←
        update_mapUpdateMapInst.has_any_in_range updates right_prefix
          right_subtree_end
      if has_left_updates
      then
        let t ← triomphe.arc.Arc.Insts.CoreOpsDerefDeref.deref left
        let r ←
          tree.Tree.with_updated_leaves ValueInst update_mapUpdateMapInst t
            updates prefix1 new_depth hashes
        let cf ← core.result.Result.Insts.CoreOpsTry.branch r
        match cf with
        | core.ops.control_flow.ControlFlow.Continue val =>
          if has_right_updates
          then
            let t1 ← triomphe.arc.Arc.Insts.CoreOpsDerefDeref.deref right
            let r1 ←
              tree.Tree.with_updated_leaves ValueInst update_mapUpdateMapInst
                t1 updates right_prefix new_depth hashes
            let cf1 ← core.result.Result.Insts.CoreOpsTry.branch r1
            match cf1 with
            | core.ops.control_flow.ControlFlow.Continue val1 =>
              let a ← tree.Tree.node ValueInst val val1 hash
              ok (core.result.Result.Ok a)
            | core.ops.control_flow.ControlFlow.Break residual =>
              core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                (triomphe.arc.Arc (tree.Tree T)) (core.convert.FromSame
                error.Error) residual
          else
            let new_right ← triomphe.arc.Arc.Insts.CoreCloneClone.clone right
            let a ← tree.Tree.node ValueInst val new_right hash
            ok (core.result.Result.Ok a)
        | core.ops.control_flow.ControlFlow.Break residual =>
          core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
            (triomphe.arc.Arc (tree.Tree T)) (core.convert.FromSame
            error.Error) residual
      else
        if has_right_updates
        then
          let new_left ← triomphe.arc.Arc.Insts.CoreCloneClone.clone left
          let t ← triomphe.arc.Arc.Insts.CoreOpsDerefDeref.deref right
          let r ←
            tree.Tree.with_updated_leaves ValueInst update_mapUpdateMapInst t
              updates right_prefix new_depth hashes
          let cf ← core.result.Result.Insts.CoreOpsTry.branch r
          match cf with
          | core.ops.control_flow.ControlFlow.Continue val =>
            let a ← tree.Tree.node ValueInst new_left val hash
            ok (core.result.Result.Ok a)
          | core.ops.control_flow.ControlFlow.Break residual =>
            core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
              (triomphe.arc.Arc (tree.Tree T)) (core.convert.FromSame
              error.Error) residual
        else
          ok (core.result.Result.Err (error.Error.NodeUpdatesMissing prefix1))
    else ok (core.result.Result.Err error.Error.UpdateLeavesError)
  | tree.Tree.Zero zero_depth =>
    if zero_depth = depth
    then
      if depth = 0#usize
      then
        let o1 ← utils.opt_packing_factor ValueInst.tree_hashTreeHashInst
        let b := core.option.Option.is_some o1
        if b
        then
          let pl ←
            packed_leaf.PackedLeaf.empty ValueInst.tree_hashTreeHashInst
              ValueInst.corecloneCloneInst
          let r ←
            packed_leaf.PackedLeaf.update ValueInst.tree_hashTreeHashInst
              ValueInst.corecloneCloneInst update_mapUpdateMapInst pl prefix1
              hash updates
          let cf ← core.result.Result.Insts.CoreOpsTry.branch r
          match cf with
          | core.ops.control_flow.ControlFlow.Continue val =>
            let a ← triomphe.arc.Arc.new (tree.Tree.PackedLeaf val)
            ok (core.result.Result.Ok a)
          | core.ops.control_flow.ControlFlow.Break residual =>
            core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
              (triomphe.arc.Arc (tree.Tree T)) (core.convert.FromSame
              error.Error) residual
        else
          let o2 ← update_mapUpdateMapInst.get updates prefix1
          let o3 ←
            core.option.OptionShared0T.cloned ValueInst.corecloneCloneInst o2
          let r ←
            core.option.Option.ok_or o3 (error.Error.LeafUpdateMissing prefix1)
          let cf ← core.result.Result.Insts.CoreOpsTry.branch r
          match cf with
          | core.ops.control_flow.ControlFlow.Continue val =>
            let a ← tree.Tree.leaf_with_hash ValueInst val hash
            ok (core.result.Result.Ok a)
          | core.ops.control_flow.ControlFlow.Break residual =>
            core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
              (triomphe.arc.Arc (tree.Tree T)) (core.convert.FromSame
              error.Error) residual
      else
        let i ← depth - 1#usize
        let new_zero ← tree.Tree.zero ValueInst i
        let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone new_zero
        let a1 ← tree.Tree.node ValueInst a new_zero hash
        let t ← triomphe.arc.Arc.Insts.CoreOpsDerefDeref.deref a1
        tree.Tree.with_updated_leaves ValueInst update_mapUpdateMapInst t
          updates prefix1 depth hashes
    else ok (core.result.Result.Err error.Error.UpdateLeavesError)
partial_fixpoint

/-- [milhouse::tree::{milhouse::tree::Tree<T>}::compute_len]:
    Source: 'src/tree.rs', lines 236:4-243:5
    Visibility: public -/
def tree.Tree.compute_len
  {T : Type} (ValueInst : Value T) (self : tree.Tree T) :
  Result Std.Usize
  := do
  match self with
  | tree.Tree.Leaf _ => ok 1#usize
  | tree.Tree.PackedLeaf leaf => ok (alloc.vec.Vec.len leaf.values)
  | tree.Tree.Node _ left right =>
    let t ← triomphe.arc.Arc.Insts.CoreOpsDerefDeref.deref left
    let i ← tree.Tree.compute_len ValueInst t
    let t1 ← triomphe.arc.Arc.Insts.CoreOpsDerefDeref.deref right
    let i1 ← tree.Tree.compute_len ValueInst t1
    i + i1
  | tree.Tree.Zero _ => ok 0#usize
partial_fixpoint

/-- [milhouse::utils::{impl core::cmp::Ord for milhouse::utils::Length}::cmp]:
    Source: 'src/utils.rs', lines 23:43-23:46
    Visibility: public -/
def utils.Length.Insts.CoreCmpOrd.cmp
  (self : utils.Length) (other : utils.Length) : Result Ordering := do
  ok (core.cmp.impls.OrdUsize.cmp self other)

/-- [milhouse::utils::{impl core::cmp::PartialOrd<milhouse::utils::Length> for milhouse::utils::Length}::partial_cmp]:
    Source: 'src/utils.rs', lines 23:31-23:41
    Visibility: public -/
def utils.Length.Insts.CoreCmpPartialOrdLength.partial_cmp
  (self : utils.Length) (other : utils.Length) : Result (Option Ordering) := do
  let o ← utils.Length.Insts.CoreCmpOrd.cmp self other
  ok (some o)

/-- [milhouse::utils::{impl core::cmp::PartialEq<milhouse::utils::Length> for milhouse::utils::Length}::eq]:
    Source: 'src/utils.rs', lines 23:16-23:25
    Visibility: public -/
def utils.Length.Insts.CoreCmpPartialEqLength.eq
  (self : utils.Length) (other : utils.Length) : Result Bool := do
  ok (self = other)

/-- Trait implementation: [milhouse::utils::{impl core::cmp::PartialEq<milhouse::utils::Length> for milhouse::utils::Length}]
    Source: 'src/utils.rs', lines 23:16-23:25 -/
@[reducible]
def utils.Length.Insts.CoreCmpPartialEqLength : core.cmp.PartialEq utils.Length
  utils.Length := {
  eq := utils.Length.Insts.CoreCmpPartialEqLength.eq
}

/-- Trait implementation: [milhouse::utils::{impl core::cmp::PartialOrd<milhouse::utils::Length> for milhouse::utils::Length}]
    Source: 'src/utils.rs', lines 23:31-23:41 -/
@[reducible]
def utils.Length.Insts.CoreCmpPartialOrdLength : core.cmp.PartialOrd
  utils.Length utils.Length := {
  partialEqInst := utils.Length.Insts.CoreCmpPartialEqLength
  partial_cmp := utils.Length.Insts.CoreCmpPartialOrdLength.partial_cmp
}

/-- [milhouse::utils::{impl core::cmp::Eq for milhouse::utils::Length}::assert_fields_are_eq]:
    Source: 'src/utils.rs', lines 23:27-23:29
    Visibility: public -/
def utils.Length.Insts.CoreCmpEq.assert_fields_are_eq
  (self : utils.Length) : Result Unit := do
  ok ()

/-- Trait implementation: [milhouse::utils::{impl core::cmp::Eq for milhouse::utils::Length}]
    Source: 'src/utils.rs', lines 23:27-23:29 -/
@[reducible]
def utils.Length.Insts.CoreCmpEq : core.cmp.Eq utils.Length := {
  partialEqInst := utils.Length.Insts.CoreCmpPartialEqLength
  assert_fields_are_eq := utils.Length.Insts.CoreCmpEq.assert_fields_are_eq
}

/-- Trait implementation: [milhouse::utils::{impl core::cmp::Ord for milhouse::utils::Length}]
    Source: 'src/utils.rs', lines 23:43-23:46 -/
@[reducible]
def utils.Length.Insts.CoreCmpOrd : core.cmp.Ord utils.Length := {
  eqInst := utils.Length.Insts.CoreCmpEq
  partialOrdInst := utils.Length.Insts.CoreCmpPartialOrdLength
  cmp := utils.Length.Insts.CoreCmpOrd.cmp
}

/-- [milhouse::tree::{milhouse::tree::Tree<T>}::rebase_on::{impl core::ops::function::FnOnce<((milhouse::utils::Length, milhouse::utils::Length),), ((milhouse::utils::Length, milhouse::utils::Length), (milhouse::utils::Length, milhouse::utils::Length))> for milhouse::tree::{milhouse::tree::Tree<T>[TraitClause0]}::rebase_on::closure#1<'_0, T>}::call_once]:
    Source: 'src/tree.rs', lines 317:25-330:21 -/
def
  tree.Tree.rebase_on.closure_1.Insts.CoreOpsFunctionFnOnceTuplePairLengthLengthPairPairLengthLengthPairLengthLength.call_once
  {T : Type} (ValueInst : Value T) (c : tree.Tree.rebase_on.closure_1 T)
  (tupled_args : (utils.Length × utils.Length)) :
  Result ((utils.Length × utils.Length) × (utils.Length × utils.Length))
  := do
  let (orig_length, base_length) := tupled_args
  let i ← 1#usize <<< c
  let orig_left_length ←
    core.cmp.min utils.Length.Insts.CoreCmpOrd orig_length i
  let i1 ← utils.Length.as_usize orig_length
  let i2 ← utils.Length.as_usize orig_left_length
  let i3 ← i1 - i2
  let base_left_length ←
    core.cmp.min utils.Length.Insts.CoreCmpOrd base_length i
  let i4 ← utils.Length.as_usize base_length
  let i5 ← utils.Length.as_usize base_left_length
  let i6 ← i4 - i5
  ok ((orig_left_length, base_left_length), (i3, i6))

/-- Trait implementation: [milhouse::tree::{milhouse::tree::Tree<T>}::rebase_on::{impl core::ops::function::FnOnce<((milhouse::utils::Length, milhouse::utils::Length),), ((milhouse::utils::Length, milhouse::utils::Length), (milhouse::utils::Length, milhouse::utils::Length))> for milhouse::tree::{milhouse::tree::Tree<T>[TraitClause0]}::rebase_on::closure#1<'_0, T>}]
    Source: 'src/tree.rs', lines 317:25-330:21 -/
@[reducible]
def
  tree.Tree.rebase_on.closure_1.Insts.CoreOpsFunctionFnOnceTuplePairLengthLengthPairPairLengthLengthPairLengthLength
  {T : Type} (ValueInst : Value T) : core.ops.function.FnOnce
  (tree.Tree.rebase_on.closure_1 T) (utils.Length × utils.Length)
  ((utils.Length × utils.Length) × (utils.Length × utils.Length)) := {
  call_once :=
    tree.Tree.rebase_on.closure_1.Insts.CoreOpsFunctionFnOnceTuplePairLengthLengthPairPairLengthLengthPairLengthLength.call_once
    ValueInst
}

/-- [milhouse::tree::{milhouse::tree::Tree<T>}::rebase_on::{impl core::ops::function::FnOnce<((milhouse::utils::Length, milhouse::utils::Length),), bool> for milhouse::tree::{milhouse::tree::Tree<T>[TraitClause0]}::rebase_on::closure<T>}::call_once]:
    Source: 'src/tree.rs', lines 310:42-310:97 -/
def
  tree.Tree.rebase_on.closure.Insts.CoreOpsFunctionFnOnceTuplePairLengthLengthBool.call_once
  {T : Type} (ValueInst : Value T) (c : tree.Tree.rebase_on.closure T)
  (tupled_args : (utils.Length × utils.Length)) :
  Result Bool
  := do
  let (orig_length, base_length) := tupled_args
  utils.Length.Insts.CoreCmpPartialEqLength.eq orig_length base_length

/-- Trait implementation: [milhouse::tree::{milhouse::tree::Tree<T>}::rebase_on::{impl core::ops::function::FnOnce<((milhouse::utils::Length, milhouse::utils::Length),), bool> for milhouse::tree::{milhouse::tree::Tree<T>[TraitClause0]}::rebase_on::closure<T>}]
    Source: 'src/tree.rs', lines 310:42-310:97 -/
@[reducible]
def
  tree.Tree.rebase_on.closure.Insts.CoreOpsFunctionFnOnceTuplePairLengthLengthBool
  {T : Type} (ValueInst : Value T) : core.ops.function.FnOnce
  (tree.Tree.rebase_on.closure T) (utils.Length × utils.Length) Bool := {
  call_once :=
    tree.Tree.rebase_on.closure.Insts.CoreOpsFunctionFnOnceTuplePairLengthLengthBool.call_once
    ValueInst
}

/-- [milhouse::tree::{milhouse::tree::Tree<T>}::rebase_on]:
    Source: 'src/tree.rs', lines 263:4-401:5
    Visibility: public -/
def tree.Tree.rebase_on
  {T : Type} (ValueInst : Value T) (orig : triomphe.arc.Arc (tree.Tree T))
  (base : triomphe.arc.Arc (tree.Tree T))
  (lengths : Option (utils.Length × utils.Length)) (full_depth : Std.Usize) :
  Result (core.result.Result (tree.RebaseAction (tree.Tree T)) error.Error)
  := do
  let b ← triomphe.arc.Arc.ptr_eq orig base
  if b
  then ok (core.result.Result.Ok tree.RebaseAction.EqualNoop)
  else
    let t ← triomphe.arc.Arc.Insts.CoreOpsDerefDeref.deref orig
    let t1 ← triomphe.arc.Arc.Insts.CoreOpsDerefDeref.deref base
    match t with
    | tree.Tree.Leaf l1 =>
      match t1 with
      | tree.Tree.Leaf l2 =>
        let b1 ←
          triomphe.arc.Arc.Insts.CoreCmpPartialEqArc.eq
            ValueInst.corecmpPartialEqInst l1.value l2.value
        if b1
        then ok (core.result.Result.Ok (tree.RebaseAction.EqualReplace base))
        else ok (core.result.Result.Ok tree.RebaseAction.NotEqualNoop)
      | tree.Tree.PackedLeaf _ =>
        ok (core.result.Result.Err error.Error.InvalidRebaseLeaf)
      | tree.Tree.Node _ _ _ =>
        ok (core.result.Result.Err error.Error.InvalidRebaseLeaf)
      | tree.Tree.Zero _ =>
        ok (core.result.Result.Ok tree.RebaseAction.NotEqualNoop)
    | tree.Tree.PackedLeaf l1 =>
      match t1 with
      | tree.Tree.Leaf _ =>
        ok (core.result.Result.Err error.Error.InvalidRebaseLeaf)
      | tree.Tree.PackedLeaf l2 =>
        let b1 ←
          alloc.vec.partial_eq.PartialEqVec.eq ValueInst.corecmpPartialEqInst
            l1.values l2.values
        if b1
        then ok (core.result.Result.Ok (tree.RebaseAction.EqualReplace base))
        else ok (core.result.Result.Ok tree.RebaseAction.NotEqualNoop)
      | tree.Tree.Node _ _ _ =>
        ok (core.result.Result.Err error.Error.InvalidRebaseLeaf)
      | tree.Tree.Zero _ =>
        ok (core.result.Result.Ok tree.RebaseAction.NotEqualNoop)
    | tree.Tree.Node orig_hash_lock l1 r1 =>
      match t1 with
      | tree.Tree.Leaf _ =>
        ok (core.result.Result.Err error.Error.InvalidRebaseLeaf)
      | tree.Tree.PackedLeaf _ =>
        ok (core.result.Result.Err error.Error.InvalidRebaseLeaf)
      | tree.Tree.Node base_hash_lock l2 r2 =>
        if full_depth > 0#usize
        then
          let rlrg ←
            lock_api.rwlock.RwLock.read
              parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
              orig_hash_lock
          let orig_hash ←
            lock_api.rwlock.RwLockReadGuard.Insts.CoreOpsDerefDeref.deref
              parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
              rlrg
          let rlrg1 ←
            lock_api.rwlock.RwLock.read
              parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
              base_hash_lock
          let base_hash ←
            lock_api.rwlock.RwLockReadGuard.Insts.CoreOpsDerefDeref.deref
              parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
              rlrg1
          let b1 ← alloy_primitives.bits.fixed.FixedBytes.is_zero orig_hash
          if b1
          then
            let new_full_depth ← full_depth - 1#usize
            let o ←
              core.option.Option.map
                (tree.Tree.rebase_on.closure_1.Insts.CoreOpsFunctionFnOnceTuplePairLengthLengthPairPairLengthLengthPairLengthLength
                ValueInst) lengths new_full_depth
            let (left_lengths, right_lengths) ←
              core.option.OptionPair.unzip o
            let r ←
              tree.Tree.rebase_on ValueInst l1 l2 left_lengths new_full_depth
            let cf ← core.result.Result.Insts.CoreOpsTry.branch r
            match cf with
            | core.ops.control_flow.ControlFlow.Continue val =>
              let r3 ←
                tree.Tree.rebase_on ValueInst r1 r2 right_lengths
                  new_full_depth
              let cf1 ← core.result.Result.Insts.CoreOpsTry.branch r3
              match cf1 with
              | core.ops.control_flow.ControlFlow.Continue val1 =>
                match val with
                | tree.RebaseAction.NotEqualNoop =>
                  match val1 with
                  | tree.RebaseAction.NotEqualNoop =>
                    ok (core.result.Result.Ok tree.RebaseAction.NotEqualNoop)
                  | tree.RebaseAction.NotEqualReplace new_right =>
                    let rl ←
                      lock_api.rwlock.RwLock.new
                        parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                        orig_hash
                    let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone l1
                    let a1 ←
                      triomphe.arc.Arc.new (tree.Tree.Node rl a new_right)
                    ok (core.result.Result.Ok
                      (tree.RebaseAction.NotEqualReplace a1))
                  | tree.RebaseAction.EqualNoop =>
                    ok (core.result.Result.Ok tree.RebaseAction.NotEqualNoop)
                  | tree.RebaseAction.EqualReplace new_right =>
                    let rl ←
                      lock_api.rwlock.RwLock.new
                        parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                        orig_hash
                    let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone l1
                    let a1 ←
                      triomphe.arc.Arc.Insts.CoreCloneClone.clone new_right
                    let a2 ← triomphe.arc.Arc.new (tree.Tree.Node rl a a1)
                    ok (core.result.Result.Ok
                      (tree.RebaseAction.NotEqualReplace a2))
                | tree.RebaseAction.NotEqualReplace new_left =>
                  match val1 with
                  | tree.RebaseAction.NotEqualNoop =>
                    let rl ←
                      lock_api.rwlock.RwLock.new
                        parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                        orig_hash
                    let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone r1
                    let a1 ←
                      triomphe.arc.Arc.new (tree.Tree.Node rl new_left a)
                    ok (core.result.Result.Ok
                      (tree.RebaseAction.NotEqualReplace a1))
                  | tree.RebaseAction.NotEqualReplace new_right =>
                    let rl ←
                      lock_api.rwlock.RwLock.new
                        parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                        orig_hash
                    let a ←
                      triomphe.arc.Arc.new (tree.Tree.Node rl new_left
                        new_right)
                    ok (core.result.Result.Ok
                      (tree.RebaseAction.NotEqualReplace a))
                  | tree.RebaseAction.EqualNoop =>
                    let rl ←
                      lock_api.rwlock.RwLock.new
                        parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                        orig_hash
                    let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone r1
                    let a1 ←
                      triomphe.arc.Arc.new (tree.Tree.Node rl new_left a)
                    ok (core.result.Result.Ok
                      (tree.RebaseAction.NotEqualReplace a1))
                  | tree.RebaseAction.EqualReplace new_right =>
                    let rl ←
                      lock_api.rwlock.RwLock.new
                        parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                        orig_hash
                    let a ←
                      triomphe.arc.Arc.Insts.CoreCloneClone.clone new_right
                    let a1 ←
                      triomphe.arc.Arc.new (tree.Tree.Node rl new_left a)
                    ok (core.result.Result.Ok
                      (tree.RebaseAction.NotEqualReplace a1))
                | tree.RebaseAction.EqualNoop =>
                  match val1 with
                  | tree.RebaseAction.NotEqualNoop =>
                    ok (core.result.Result.Ok tree.RebaseAction.NotEqualNoop)
                  | tree.RebaseAction.NotEqualReplace new_right =>
                    let rl ←
                      lock_api.rwlock.RwLock.new
                        parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                        orig_hash
                    let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone l1
                    let a1 ←
                      triomphe.arc.Arc.new (tree.Tree.Node rl a new_right)
                    ok (core.result.Result.Ok
                      (tree.RebaseAction.NotEqualReplace a1))
                  | tree.RebaseAction.EqualNoop =>
                    ok (core.result.Result.Ok tree.RebaseAction.EqualNoop)
                  | tree.RebaseAction.EqualReplace new_right =>
                    let rl ←
                      lock_api.rwlock.RwLock.new
                        parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                        orig_hash
                    let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone l1
                    let a1 ←
                      triomphe.arc.Arc.Insts.CoreCloneClone.clone new_right
                    let a2 ← triomphe.arc.Arc.new (tree.Tree.Node rl a a1)
                    ok (core.result.Result.Ok
                      (tree.RebaseAction.NotEqualReplace a2))
                | tree.RebaseAction.EqualReplace new_left =>
                  match val1 with
                  | tree.RebaseAction.NotEqualNoop =>
                    let rl ←
                      lock_api.rwlock.RwLock.new
                        parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                        orig_hash
                    let a ←
                      triomphe.arc.Arc.Insts.CoreCloneClone.clone new_left
                    let a1 ← triomphe.arc.Arc.Insts.CoreCloneClone.clone r1
                    let a2 ← triomphe.arc.Arc.new (tree.Tree.Node rl a a1)
                    ok (core.result.Result.Ok
                      (tree.RebaseAction.NotEqualReplace a2))
                  | tree.RebaseAction.NotEqualReplace new_right =>
                    let rl ←
                      lock_api.rwlock.RwLock.new
                        parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                        orig_hash
                    let a ←
                      triomphe.arc.Arc.Insts.CoreCloneClone.clone new_left
                    let a1 ←
                      triomphe.arc.Arc.new (tree.Tree.Node rl a new_right)
                    ok (core.result.Result.Ok
                      (tree.RebaseAction.NotEqualReplace a1))
                  | tree.RebaseAction.EqualNoop =>
                    ok (core.result.Result.Ok (tree.RebaseAction.EqualReplace
                      base))
                  | tree.RebaseAction.EqualReplace _ =>
                    ok (core.result.Result.Ok (tree.RebaseAction.EqualReplace
                      base))
              | core.ops.control_flow.ControlFlow.Break residual =>
                core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                  (tree.RebaseAction (tree.Tree T)) (core.convert.FromSame
                  error.Error) residual
            | core.ops.control_flow.ControlFlow.Break residual =>
              core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                (tree.RebaseAction (tree.Tree T)) (core.convert.FromSame
                error.Error) residual
          else
            let b2 ←
              alloy_primitives.bits.fixed.FixedBytes.Insts.CoreCmpPartialEqFixedBytes.eq
                orig_hash base_hash
            if b2
            then
              let b3 ←
                core.option.Option.is_none_or
                  (tree.Tree.rebase_on.closure.Insts.CoreOpsFunctionFnOnceTuplePairLengthLengthBool
                  ValueInst) lengths ()
              if b3
              then
                ok (core.result.Result.Ok (tree.RebaseAction.EqualReplace
                  base))
              else
                let new_full_depth ← full_depth - 1#usize
                let o ←
                  core.option.Option.map
                    (tree.Tree.rebase_on.closure_1.Insts.CoreOpsFunctionFnOnceTuplePairLengthLengthPairPairLengthLengthPairLengthLength
                    ValueInst) lengths new_full_depth
                let (left_lengths, right_lengths) ←
                  core.option.OptionPair.unzip o
                let r ←
                  tree.Tree.rebase_on ValueInst l1 l2 left_lengths
                    new_full_depth
                let cf ← core.result.Result.Insts.CoreOpsTry.branch r
                match cf with
                | core.ops.control_flow.ControlFlow.Continue val =>
                  let r3 ←
                    tree.Tree.rebase_on ValueInst r1 r2 right_lengths
                      new_full_depth
                  let cf1 ← core.result.Result.Insts.CoreOpsTry.branch r3
                  match cf1 with
                  | core.ops.control_flow.ControlFlow.Continue val1 =>
                    match val with
                    | tree.RebaseAction.NotEqualNoop =>
                      match val1 with
                      | tree.RebaseAction.NotEqualNoop =>
                        ok (core.result.Result.Ok
                          tree.RebaseAction.NotEqualNoop)
                      | tree.RebaseAction.NotEqualReplace new_right =>
                        let rl ←
                          lock_api.rwlock.RwLock.new
                            parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                            orig_hash
                        let a ←
                          triomphe.arc.Arc.Insts.CoreCloneClone.clone l1
                        let a1 ←
                          triomphe.arc.Arc.new (tree.Tree.Node rl a new_right)
                        ok (core.result.Result.Ok
                          (tree.RebaseAction.NotEqualReplace a1))
                      | tree.RebaseAction.EqualNoop =>
                        ok (core.result.Result.Ok
                          tree.RebaseAction.NotEqualNoop)
                      | tree.RebaseAction.EqualReplace new_right =>
                        let rl ←
                          lock_api.rwlock.RwLock.new
                            parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                            orig_hash
                        let a ←
                          triomphe.arc.Arc.Insts.CoreCloneClone.clone l1
                        let a1 ←
                          triomphe.arc.Arc.Insts.CoreCloneClone.clone new_right
                        let a2 ←
                          triomphe.arc.Arc.new (tree.Tree.Node rl a a1)
                        ok (core.result.Result.Ok
                          (tree.RebaseAction.NotEqualReplace a2))
                    | tree.RebaseAction.NotEqualReplace new_left =>
                      match val1 with
                      | tree.RebaseAction.NotEqualNoop =>
                        let rl ←
                          lock_api.rwlock.RwLock.new
                            parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                            orig_hash
                        let a ←
                          triomphe.arc.Arc.Insts.CoreCloneClone.clone r1
                        let a1 ←
                          triomphe.arc.Arc.new (tree.Tree.Node rl new_left a)
                        ok (core.result.Result.Ok
                          (tree.RebaseAction.NotEqualReplace a1))
                      | tree.RebaseAction.NotEqualReplace new_right =>
                        let rl ←
                          lock_api.rwlock.RwLock.new
                            parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                            orig_hash
                        let a ←
                          triomphe.arc.Arc.new (tree.Tree.Node rl new_left
                            new_right)
                        ok (core.result.Result.Ok
                          (tree.RebaseAction.NotEqualReplace a))
                      | tree.RebaseAction.EqualNoop =>
                        let rl ←
                          lock_api.rwlock.RwLock.new
                            parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                            orig_hash
                        let a ←
                          triomphe.arc.Arc.Insts.CoreCloneClone.clone r1
                        let a1 ←
                          triomphe.arc.Arc.new (tree.Tree.Node rl new_left a)
                        ok (core.result.Result.Ok
                          (tree.RebaseAction.NotEqualReplace a1))
                      | tree.RebaseAction.EqualReplace new_right =>
                        let rl ←
                          lock_api.rwlock.RwLock.new
                            parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                            orig_hash
                        let a ←
                          triomphe.arc.Arc.Insts.CoreCloneClone.clone new_right
                        let a1 ←
                          triomphe.arc.Arc.new (tree.Tree.Node rl new_left a)
                        ok (core.result.Result.Ok
                          (tree.RebaseAction.NotEqualReplace a1))
                    | tree.RebaseAction.EqualNoop =>
                      match val1 with
                      | tree.RebaseAction.NotEqualNoop =>
                        ok (core.result.Result.Ok
                          tree.RebaseAction.NotEqualNoop)
                      | tree.RebaseAction.NotEqualReplace new_right =>
                        let rl ←
                          lock_api.rwlock.RwLock.new
                            parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                            orig_hash
                        let a ←
                          triomphe.arc.Arc.Insts.CoreCloneClone.clone l1
                        let a1 ←
                          triomphe.arc.Arc.new (tree.Tree.Node rl a new_right)
                        ok (core.result.Result.Ok
                          (tree.RebaseAction.NotEqualReplace a1))
                      | tree.RebaseAction.EqualNoop =>
                        ok (core.result.Result.Ok tree.RebaseAction.EqualNoop)
                      | tree.RebaseAction.EqualReplace new_right =>
                        let rl ←
                          lock_api.rwlock.RwLock.new
                            parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                            orig_hash
                        let a ←
                          triomphe.arc.Arc.Insts.CoreCloneClone.clone l1
                        let a1 ←
                          triomphe.arc.Arc.Insts.CoreCloneClone.clone new_right
                        let a2 ←
                          triomphe.arc.Arc.new (tree.Tree.Node rl a a1)
                        ok (core.result.Result.Ok
                          (tree.RebaseAction.NotEqualReplace a2))
                    | tree.RebaseAction.EqualReplace new_left =>
                      match val1 with
                      | tree.RebaseAction.NotEqualNoop =>
                        let rl ←
                          lock_api.rwlock.RwLock.new
                            parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                            orig_hash
                        let a ←
                          triomphe.arc.Arc.Insts.CoreCloneClone.clone new_left
                        let a1 ←
                          triomphe.arc.Arc.Insts.CoreCloneClone.clone r1
                        let a2 ←
                          triomphe.arc.Arc.new (tree.Tree.Node rl a a1)
                        ok (core.result.Result.Ok
                          (tree.RebaseAction.NotEqualReplace a2))
                      | tree.RebaseAction.NotEqualReplace new_right =>
                        let rl ←
                          lock_api.rwlock.RwLock.new
                            parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                            orig_hash
                        let a ←
                          triomphe.arc.Arc.Insts.CoreCloneClone.clone new_left
                        let a1 ←
                          triomphe.arc.Arc.new (tree.Tree.Node rl a new_right)
                        ok (core.result.Result.Ok
                          (tree.RebaseAction.NotEqualReplace a1))
                      | tree.RebaseAction.EqualNoop =>
                        ok (core.result.Result.Ok
                          (tree.RebaseAction.EqualReplace base))
                      | tree.RebaseAction.EqualReplace _ =>
                        ok (core.result.Result.Ok
                          (tree.RebaseAction.EqualReplace base))
                  | core.ops.control_flow.ControlFlow.Break residual =>
                    core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                      (tree.RebaseAction (tree.Tree T)) (core.convert.FromSame
                      error.Error) residual
                | core.ops.control_flow.ControlFlow.Break residual =>
                  core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                    (tree.RebaseAction (tree.Tree T)) (core.convert.FromSame
                    error.Error) residual
            else
              let new_full_depth ← full_depth - 1#usize
              let o ←
                core.option.Option.map
                  (tree.Tree.rebase_on.closure_1.Insts.CoreOpsFunctionFnOnceTuplePairLengthLengthPairPairLengthLengthPairLengthLength
                  ValueInst) lengths new_full_depth
              let (left_lengths, right_lengths) ←
                core.option.OptionPair.unzip o
              let r ←
                tree.Tree.rebase_on ValueInst l1 l2 left_lengths new_full_depth
              let cf ← core.result.Result.Insts.CoreOpsTry.branch r
              match cf with
              | core.ops.control_flow.ControlFlow.Continue val =>
                let r3 ←
                  tree.Tree.rebase_on ValueInst r1 r2 right_lengths
                    new_full_depth
                let cf1 ← core.result.Result.Insts.CoreOpsTry.branch r3
                match cf1 with
                | core.ops.control_flow.ControlFlow.Continue val1 =>
                  match val with
                  | tree.RebaseAction.NotEqualNoop =>
                    match val1 with
                    | tree.RebaseAction.NotEqualNoop =>
                      ok (core.result.Result.Ok tree.RebaseAction.NotEqualNoop)
                    | tree.RebaseAction.NotEqualReplace new_right =>
                      let rl ←
                        lock_api.rwlock.RwLock.new
                          parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                          orig_hash
                      let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone l1
                      let a1 ←
                        triomphe.arc.Arc.new (tree.Tree.Node rl a new_right)
                      ok (core.result.Result.Ok
                        (tree.RebaseAction.NotEqualReplace a1))
                    | tree.RebaseAction.EqualNoop =>
                      ok (core.result.Result.Ok tree.RebaseAction.NotEqualNoop)
                    | tree.RebaseAction.EqualReplace new_right =>
                      let rl ←
                        lock_api.rwlock.RwLock.new
                          parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                          orig_hash
                      let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone l1
                      let a1 ←
                        triomphe.arc.Arc.Insts.CoreCloneClone.clone new_right
                      let a2 ← triomphe.arc.Arc.new (tree.Tree.Node rl a a1)
                      ok (core.result.Result.Ok
                        (tree.RebaseAction.NotEqualReplace a2))
                  | tree.RebaseAction.NotEqualReplace new_left =>
                    match val1 with
                    | tree.RebaseAction.NotEqualNoop =>
                      let rl ←
                        lock_api.rwlock.RwLock.new
                          parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                          orig_hash
                      let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone r1
                      let a1 ←
                        triomphe.arc.Arc.new (tree.Tree.Node rl new_left a)
                      ok (core.result.Result.Ok
                        (tree.RebaseAction.NotEqualReplace a1))
                    | tree.RebaseAction.NotEqualReplace new_right =>
                      let rl ←
                        lock_api.rwlock.RwLock.new
                          parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                          orig_hash
                      let a ←
                        triomphe.arc.Arc.new (tree.Tree.Node rl new_left
                          new_right)
                      ok (core.result.Result.Ok
                        (tree.RebaseAction.NotEqualReplace a))
                    | tree.RebaseAction.EqualNoop =>
                      let rl ←
                        lock_api.rwlock.RwLock.new
                          parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                          orig_hash
                      let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone r1
                      let a1 ←
                        triomphe.arc.Arc.new (tree.Tree.Node rl new_left a)
                      ok (core.result.Result.Ok
                        (tree.RebaseAction.NotEqualReplace a1))
                    | tree.RebaseAction.EqualReplace new_right =>
                      let rl ←
                        lock_api.rwlock.RwLock.new
                          parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                          orig_hash
                      let a ←
                        triomphe.arc.Arc.Insts.CoreCloneClone.clone new_right
                      let a1 ←
                        triomphe.arc.Arc.new (tree.Tree.Node rl new_left a)
                      ok (core.result.Result.Ok
                        (tree.RebaseAction.NotEqualReplace a1))
                  | tree.RebaseAction.EqualNoop =>
                    match val1 with
                    | tree.RebaseAction.NotEqualNoop =>
                      ok (core.result.Result.Ok tree.RebaseAction.NotEqualNoop)
                    | tree.RebaseAction.NotEqualReplace new_right =>
                      let rl ←
                        lock_api.rwlock.RwLock.new
                          parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                          orig_hash
                      let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone l1
                      let a1 ←
                        triomphe.arc.Arc.new (tree.Tree.Node rl a new_right)
                      ok (core.result.Result.Ok
                        (tree.RebaseAction.NotEqualReplace a1))
                    | tree.RebaseAction.EqualNoop =>
                      ok (core.result.Result.Ok tree.RebaseAction.EqualNoop)
                    | tree.RebaseAction.EqualReplace new_right =>
                      let rl ←
                        lock_api.rwlock.RwLock.new
                          parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                          orig_hash
                      let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone l1
                      let a1 ←
                        triomphe.arc.Arc.Insts.CoreCloneClone.clone new_right
                      let a2 ← triomphe.arc.Arc.new (tree.Tree.Node rl a a1)
                      ok (core.result.Result.Ok
                        (tree.RebaseAction.NotEqualReplace a2))
                  | tree.RebaseAction.EqualReplace new_left =>
                    match val1 with
                    | tree.RebaseAction.NotEqualNoop =>
                      let rl ←
                        lock_api.rwlock.RwLock.new
                          parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                          orig_hash
                      let a ←
                        triomphe.arc.Arc.Insts.CoreCloneClone.clone new_left
                      let a1 ← triomphe.arc.Arc.Insts.CoreCloneClone.clone r1
                      let a2 ← triomphe.arc.Arc.new (tree.Tree.Node rl a a1)
                      ok (core.result.Result.Ok
                        (tree.RebaseAction.NotEqualReplace a2))
                    | tree.RebaseAction.NotEqualReplace new_right =>
                      let rl ←
                        lock_api.rwlock.RwLock.new
                          parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
                          orig_hash
                      let a ←
                        triomphe.arc.Arc.Insts.CoreCloneClone.clone new_left
                      let a1 ←
                        triomphe.arc.Arc.new (tree.Tree.Node rl a new_right)
                      ok (core.result.Result.Ok
                        (tree.RebaseAction.NotEqualReplace a1))
                    | tree.RebaseAction.EqualNoop =>
                      ok (core.result.Result.Ok (tree.RebaseAction.EqualReplace
                        base))
                    | tree.RebaseAction.EqualReplace _ =>
                      ok (core.result.Result.Ok (tree.RebaseAction.EqualReplace
                        base))
                | core.ops.control_flow.ControlFlow.Break residual =>
                  core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                    (tree.RebaseAction (tree.Tree T)) (core.convert.FromSame
                    error.Error) residual
              | core.ops.control_flow.ControlFlow.Break residual =>
                core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                  (tree.RebaseAction (tree.Tree T)) (core.convert.FromSame
                  error.Error) residual
        else ok (core.result.Result.Err error.Error.InvalidRebaseNode)
      | tree.Tree.Zero _ =>
        ok (core.result.Result.Ok tree.RebaseAction.NotEqualNoop)
    | tree.Tree.Zero z1 =>
      match t1 with
      | tree.Tree.Leaf _ =>
        ok (core.result.Result.Ok tree.RebaseAction.NotEqualNoop)
      | tree.Tree.PackedLeaf _ =>
        ok (core.result.Result.Ok tree.RebaseAction.NotEqualNoop)
      | tree.Tree.Node _ _ _ =>
        ok (core.result.Result.Ok tree.RebaseAction.NotEqualNoop)
      | tree.Tree.Zero z2 =>
        let b1 ← lift (core.cmp.impls.PartialEqUsize.eq z1 z2)
        if b1
        then ok (core.result.Result.Ok (tree.RebaseAction.EqualReplace base))
        else ok (core.result.Result.Ok tree.RebaseAction.NotEqualNoop)
partial_fixpoint

/-- [milhouse::tree::{milhouse::tree::Tree<T>}::intra_rebase]:
    Source: 'src/tree.rs', lines 438:4-522:5
    Visibility: public -/
def tree.Tree.intra_rebase
  {T : Type} (ValueInst : Value T) (orig : triomphe.arc.Arc (tree.Tree T))
  (known_subtrees : std.collections.hash.map.HashMap (Std.Usize ×
  (alloy_primitives.bits.fixed.FixedBytes 32#usize)) (triomphe.arc.Arc
  (tree.Tree T)) std.hash.random.RandomState Global)
  (current_depth : Std.Usize) (packing_depth : Std.Usize) (len : utils.Length)
  :
  Result ((core.result.Result (tree.IntraRebaseAction (tree.Tree T))
    error.Error) × (std.collections.hash.map.HashMap (Std.Usize ×
    (alloy_primitives.bits.fixed.FixedBytes 32#usize)) (triomphe.arc.Arc
    (tree.Tree T)) std.hash.random.RandomState Global))
  := do
  let t ← triomphe.arc.Arc.Insts.CoreOpsDerefDeref.deref orig
  match t with
  | tree.Tree.Leaf _ =>
    ok (core.result.Result.Ok tree.IntraRebaseAction.Noop, known_subtrees)
  | tree.Tree.PackedLeaf _ =>
    ok (core.result.Result.Ok tree.IntraRebaseAction.Noop, known_subtrees)
  | tree.Tree.Node hash left right =>
    if current_depth > 0#usize
    then
      let rlrg ←
        lock_api.rwlock.RwLock.read
          parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
          hash
      let hash1 ←
        lock_api.rwlock.RwLockReadGuard.Insts.CoreOpsDerefDeref.deref
          parking_lot.raw_rwlock.RawRwLock.Insts.Lock_apiRwlockRawRwLockGuardNoSend
          rlrg
      let b ← alloy_primitives.bits.fixed.FixedBytes.is_zero hash1
      if b
      then
        ok (core.result.Result.Err error.Error.IntraRebaseZeroHash,
          known_subtrees)
      else
        let i ← current_depth + packing_depth
        let capacity ← 1#usize <<< i
        let i1 ← utils.Length.as_usize len
        if i1 = capacity
        then
          let o ←
            std.collections.hash.map.HashMap.get (Pair.Insts.CoreCmpEq
              core.cmp.EqUsize
              (alloy_primitives.bits.fixed.FixedBytes.Insts.CoreCmpEq
              32#usize)) (Pair.Insts.CoreHashHash Usize.Insts.CoreHashHash
              (alloy_primitives.bits.fixed.FixedBytes.Insts.CoreHashHash
              32#usize))
              std.hash.random.RandomState.Insts.CoreHashBuildHasherDefaultHasher
              (core.borrow.Borrow.Blanket (Std.Usize ×
              (alloy_primitives.bits.fixed.FixedBytes 32#usize)))
              (Pair.Insts.CoreHashHash Usize.Insts.CoreHashHash
              (alloy_primitives.bits.fixed.FixedBytes.Insts.CoreHashHash
              32#usize)) (Pair.Insts.CoreCmpEq core.cmp.EqUsize
              (alloy_primitives.bits.fixed.FixedBytes.Insts.CoreCmpEq
              32#usize)) known_subtrees (current_depth, hash1)
          match o with
          | none =>
            let i2 ← current_depth - 1#usize
            let i3 ← i2 + packing_depth
            let child_capacity ← 1#usize <<< i3
            let left_len ←
              core.cmp.min utils.Length.Insts.CoreCmpOrd len child_capacity
            let i4 ← utils.Length.as_usize left_len
            let i5 ← i1 - i4
            let (r, known_subtrees1) ←
              tree.Tree.intra_rebase ValueInst left known_subtrees i2
                packing_depth left_len
            let cf ← core.result.Result.Insts.CoreOpsTry.branch r
            match cf with
            | core.ops.control_flow.ControlFlow.Continue val =>
              let (r1, known_subtrees2) ←
                tree.Tree.intra_rebase ValueInst right known_subtrees1 i2
                  packing_depth i5
              let cf1 ← core.result.Result.Insts.CoreOpsTry.branch r1
              match cf1 with
              | core.ops.control_flow.ControlFlow.Continue val1 =>
                match val with
                | tree.IntraRebaseAction.Noop =>
                  let action ←
                    match val1 with
                    | tree.IntraRebaseAction.Noop =>
                      ok tree.IntraRebaseAction.Noop
                    | tree.IntraRebaseAction.Replace new_right =>
                      do
                      let a ←
                        triomphe.arc.Arc.Insts.CoreCloneClone.clone left
                      let a1 ← tree.Tree.node ValueInst a new_right hash1
                      ok (tree.IntraRebaseAction.Replace a1)
                  let new_subtree ←
                    match action with
                    | tree.IntraRebaseAction.Noop =>
                      triomphe.arc.Arc.Insts.CoreCloneClone.clone orig
                    | tree.IntraRebaseAction.Replace new =>
                      triomphe.arc.Arc.Insts.CoreCloneClone.clone new
                  let (existing_entry, known_subtrees3) ←
                    std.collections.hash.map.HashMap.insert
                      (Pair.Insts.CoreCmpEq core.cmp.EqUsize
                      (alloy_primitives.bits.fixed.FixedBytes.Insts.CoreCmpEq
                      32#usize)) (Pair.Insts.CoreHashHash
                      Usize.Insts.CoreHashHash
                      (alloy_primitives.bits.fixed.FixedBytes.Insts.CoreHashHash
                      32#usize))
                      std.hash.random.RandomState.Insts.CoreHashBuildHasherDefaultHasher
                      known_subtrees2 (current_depth, hash1) new_subtree
                  let b1 := core.option.Option.is_some existing_entry
                  if b1
                  then
                    ok (core.result.Result.Err
                      error.Error.IntraRebaseRepeatVisit, known_subtrees3)
                  else ok (core.result.Result.Ok action, known_subtrees3)
                | tree.IntraRebaseAction.Replace new_left =>
                  match val1 with
                  | tree.IntraRebaseAction.Noop =>
                    let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone right
                    let new ← tree.Tree.node ValueInst new_left a hash1
                    let new_subtree ←
                      triomphe.arc.Arc.Insts.CoreCloneClone.clone new
                    let (existing_entry, known_subtrees3) ←
                      std.collections.hash.map.HashMap.insert
                        (Pair.Insts.CoreCmpEq core.cmp.EqUsize
                        (alloy_primitives.bits.fixed.FixedBytes.Insts.CoreCmpEq
                        32#usize)) (Pair.Insts.CoreHashHash
                        Usize.Insts.CoreHashHash
                        (alloy_primitives.bits.fixed.FixedBytes.Insts.CoreHashHash
                        32#usize))
                        std.hash.random.RandomState.Insts.CoreHashBuildHasherDefaultHasher
                        known_subtrees2 (current_depth, hash1) new_subtree
                    let b1 := core.option.Option.is_some existing_entry
                    if b1
                    then
                      ok (core.result.Result.Err
                        error.Error.IntraRebaseRepeatVisit, known_subtrees3)
                    else
                      ok (core.result.Result.Ok (tree.IntraRebaseAction.Replace
                        new), known_subtrees3)
                  | tree.IntraRebaseAction.Replace new_right =>
                    let new ←
                      tree.Tree.node ValueInst new_left new_right hash1
                    let new_subtree ←
                      triomphe.arc.Arc.Insts.CoreCloneClone.clone new
                    let (existing_entry, known_subtrees3) ←
                      std.collections.hash.map.HashMap.insert
                        (Pair.Insts.CoreCmpEq core.cmp.EqUsize
                        (alloy_primitives.bits.fixed.FixedBytes.Insts.CoreCmpEq
                        32#usize)) (Pair.Insts.CoreHashHash
                        Usize.Insts.CoreHashHash
                        (alloy_primitives.bits.fixed.FixedBytes.Insts.CoreHashHash
                        32#usize))
                        std.hash.random.RandomState.Insts.CoreHashBuildHasherDefaultHasher
                        known_subtrees2 (current_depth, hash1) new_subtree
                    let b1 := core.option.Option.is_some existing_entry
                    if b1
                    then
                      ok (core.result.Result.Err
                        error.Error.IntraRebaseRepeatVisit, known_subtrees3)
                    else
                      ok (core.result.Result.Ok (tree.IntraRebaseAction.Replace
                        new), known_subtrees3)
              | core.ops.control_flow.ControlFlow.Break residual =>
                let r2 ←
                  core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                    (tree.IntraRebaseAction (tree.Tree T))
                    (core.convert.FromSame error.Error) residual
                ok (r2, known_subtrees2)
            | core.ops.control_flow.ControlFlow.Break residual =>
              let r1 ←
                core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                  (tree.IntraRebaseAction (tree.Tree T)) (core.convert.FromSame
                  error.Error) residual
              ok (r1, known_subtrees1)
          | some known_subtree =>
            let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone known_subtree
            ok (core.result.Result.Ok (tree.IntraRebaseAction.Replace a),
              known_subtrees)
        else
          let i2 ← current_depth - 1#usize
          let i3 ← i2 + packing_depth
          let child_capacity ← 1#usize <<< i3
          let left_len ←
            core.cmp.min utils.Length.Insts.CoreCmpOrd len child_capacity
          let i4 ← utils.Length.as_usize left_len
          let i5 ← i1 - i4
          let (r, known_subtrees1) ←
            tree.Tree.intra_rebase ValueInst left known_subtrees i2
              packing_depth left_len
          let cf ← core.result.Result.Insts.CoreOpsTry.branch r
          match cf with
          | core.ops.control_flow.ControlFlow.Continue val =>
            let (r1, known_subtrees2) ←
              tree.Tree.intra_rebase ValueInst right known_subtrees1 i2
                packing_depth i5
            let cf1 ← core.result.Result.Insts.CoreOpsTry.branch r1
            match cf1 with
            | core.ops.control_flow.ControlFlow.Continue val1 =>
              match val with
              | tree.IntraRebaseAction.Noop =>
                let action ←
                  match val1 with
                  | tree.IntraRebaseAction.Noop =>
                    ok tree.IntraRebaseAction.Noop
                  | tree.IntraRebaseAction.Replace new_right =>
                    do
                    let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone left
                    let a1 ← tree.Tree.node ValueInst a new_right hash1
                    ok (tree.IntraRebaseAction.Replace a1)
                ok (core.result.Result.Ok action, known_subtrees2)
              | tree.IntraRebaseAction.Replace new_left =>
                match val1 with
                | tree.IntraRebaseAction.Noop =>
                  let a ← triomphe.arc.Arc.Insts.CoreCloneClone.clone right
                  let new ← tree.Tree.node ValueInst new_left a hash1
                  ok (core.result.Result.Ok (tree.IntraRebaseAction.Replace
                    new), known_subtrees2)
                | tree.IntraRebaseAction.Replace new_right =>
                  let new ← tree.Tree.node ValueInst new_left new_right hash1
                  ok (core.result.Result.Ok (tree.IntraRebaseAction.Replace
                    new), known_subtrees2)
            | core.ops.control_flow.ControlFlow.Break residual =>
              let r2 ←
                core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                  (tree.IntraRebaseAction (tree.Tree T)) (core.convert.FromSame
                  error.Error) residual
              ok (r2, known_subtrees2)
          | core.ops.control_flow.ControlFlow.Break residual =>
            let r1 ←
              core.result.Result.Insts.CoreOpsTryTraitFromResidualResultInfallible.from_residual
                (tree.IntraRebaseAction (tree.Tree T)) (core.convert.FromSame
                error.Error) residual
            ok (r1, known_subtrees1)
    else
      ok (core.result.Result.Err error.Error.IntraRebaseZeroDepth,
        known_subtrees)
  | tree.Tree.Zero _ =>
    ok (core.result.Result.Ok tree.IntraRebaseAction.Noop, known_subtrees)
partial_fixpoint

end milhouse
