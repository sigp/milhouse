- Commit progress as you go (prove a lemma -> commit).
- Always bypass GPG signing when committing. Include a co-author tag for the model used.
- Stop and raise a warning if a bug is discovered in the Rust code which creates messy/unintuitive semantics on the Lean side.
- Don't make changes to aeneas, instead raise the limitations with the user for discussion and possible workarounds.

